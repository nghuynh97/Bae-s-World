---
phase: 02-public-portfolio
plan: 03
type: execute
wave: 2
depends_on: [02-00, 02-01]
files_modified:
  - src/app/(private)/admin/portfolio/page.tsx
  - src/app/(private)/admin/portfolio/new/page.tsx
  - src/app/(private)/admin/portfolio/[id]/edit/page.tsx
  - src/app/(private)/admin/categories/page.tsx
  - src/app/(private)/admin/about/page.tsx
  - src/components/layout/top-nav.tsx
  - src/components/layout/bottom-tab-bar.tsx
autonomous: true
requirements: [PORT-05, AUTH-06]

must_haves:
  truths:
    - "Funnghy can upload a new portfolio photo with title, description, and category"
    - "Funnghy can edit the title, description, and category of an existing portfolio photo"
    - "Funnghy can delete a portfolio photo"
    - "Boyfriend can upload portfolio photos with title, description, and category"
    - "Funnghy can create, edit, and delete categories"
    - "Funnghy can edit the about page content (bio, email, social links, profile photo)"
    - "Admin pages require authentication (rendered inside (private) route group)"
  artifacts:
    - path: "src/app/(private)/admin/portfolio/page.tsx"
      provides: "Portfolio item list with edit/delete actions"
      contains: "deletePortfolioItem"
    - path: "src/app/(private)/admin/portfolio/new/page.tsx"
      provides: "Upload new portfolio item form"
      contains: "createPortfolioItem"
    - path: "src/app/(private)/admin/categories/page.tsx"
      provides: "Category management list"
      contains: "createCategory"
    - path: "src/app/(private)/admin/about/page.tsx"
      provides: "About page content editor"
      contains: "updateAboutContent"
  key_links:
    - from: "src/app/(private)/admin/portfolio/new/page.tsx"
      to: "src/actions/portfolio.ts"
      via: "createPortfolioItem Server Action"
      pattern: "createPortfolioItem"
    - from: "src/app/(private)/admin/portfolio/new/page.tsx"
      to: "src/components/upload/image-uploader.tsx"
      via: "reuse existing upload component"
      pattern: "ImageUploader"
    - from: "src/app/(private)/admin/portfolio/page.tsx"
      to: "src/actions/portfolio.ts"
      via: "getPortfolioItems and deletePortfolioItem"
      pattern: "deletePortfolioItem"
    - from: "src/app/(private)/admin/portfolio/[id]/edit/page.tsx"
      to: "src/actions/portfolio.ts"
      via: "getPortfolioItemById and updatePortfolioItem"
      pattern: "getPortfolioItemById"
---

<objective>
Build the admin content management pages for portfolio items, categories, and about page editing.

Purpose: Enable Funnghy and her boyfriend to manage portfolio content -- upload photos, organize categories, and edit the about page -- all behind authentication.
Output: Admin pages for portfolio CRUD, category management, and about page editing, all within the (private) route group.
</objective>

<execution_context>
@C:/Users/ADMIN/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/ADMIN/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-public-portfolio/02-CONTEXT.md
@.planning/phases/02-public-portfolio/02-RESEARCH.md
@.planning/phases/02-public-portfolio/02-UI-SPEC.md
@.planning/phases/02-public-portfolio/02-01-SUMMARY.md

<interfaces>
<!-- Server Actions from Plan 01 -->

From src/actions/portfolio.ts:
```typescript
export async function getPortfolioItems(cursor?: string, categorySlug?: string): Promise<{ items: PortfolioItem[]; nextCursor: string | null }>;
export async function getPortfolioItemById(itemId: string): Promise<PortfolioItemWithDetails | null>;
export async function createPortfolioItem(data: { title: string; description?: string; categoryId: string; imageId: string }): Promise<CreatedItem>;
export async function updatePortfolioItem(itemId: string, data: { title?: string; description?: string; categoryId?: string }): Promise<UpdatedItem>;
export async function deletePortfolioItem(itemId: string): Promise<{ success: true }>;
```

From src/actions/categories.ts:
```typescript
export async function getCategories(): Promise<Category[]>;
export async function createCategory(data: { name: string }): Promise<Category>;
export async function updateCategory(categoryId: string, data: { name?: string; displayOrder?: number }): Promise<Category>;
export async function deleteCategory(categoryId: string): Promise<{ success: true }>;
```

From src/actions/about.ts:
```typescript
export async function getAboutContent(): Promise<AboutContent | null>;
export async function updateAboutContent(data: { bio?: string; email?: string; instagramUrl?: string; tiktokUrl?: string; profileImageId?: string }): Promise<AboutContent>;
```

From src/components/upload/image-uploader.tsx:
```typescript
interface ImageUploaderProps {
  bucket: "public-images" | "private-images";
  folder: string;
  onUploadComplete?: (imageId: string) => void;
}
export function ImageUploader({ bucket, folder, onUploadComplete }: ImageUploaderProps): JSX.Element;
```

From src/components/ui (available shadcn components):
- Button, Card, Input, Label, Skeleton, Separator, DropdownMenu, Dialog, Progress

From src/components/layout/top-nav.tsx:
```typescript
interface TopNavProps {
  isAuthenticated: boolean;
  userName?: string;
  userMenu?: React.ReactNode;
}
// Has navLinks array that needs "Admin" link added for authenticated users
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create portfolio admin pages (list, upload new, edit)</name>
  <files>src/app/(private)/admin/portfolio/page.tsx, src/app/(private)/admin/portfolio/new/page.tsx, src/app/(private)/admin/portfolio/[id]/edit/page.tsx</files>
  <read_first>
    - src/app/(private)/layout.tsx (private layout structure)
    - src/app/(private)/dashboard/page.tsx (existing private page pattern)
    - src/components/upload/image-uploader.tsx (reusable upload component props)
    - src/components/ui/card.tsx (Card component exports)
    - src/components/ui/button.tsx (Button component props and variants)
    - src/components/ui/input.tsx (Input component)
    - src/components/ui/dropdown-menu.tsx (DropdownMenu for actions)
    - src/components/ui/dialog.tsx (Dialog for delete confirmation)
    - src/actions/portfolio.ts (CRUD actions including getPortfolioItemById)
    - src/actions/categories.ts (getCategories for select dropdown)
    - .planning/phases/02-public-portfolio/02-UI-SPEC.md (admin content management specs, copywriting contract)
  </read_first>
  <action>
1. **Create `src/app/(private)/admin/portfolio/page.tsx`** (Server Component with client parts):
   - Fetch all portfolio items server-side: call getPortfolioItems() (no category filter, load all -- or paginate if needed)
   - Page title: "Portfolio Photos" with `className="font-display text-xl font-bold text-text-primary"`
   - "Upload Photo" button (Link to /admin/portfolio/new): `className="bg-accent text-white hover:bg-accent-hover px-4 py-2 rounded-lg text-sm"`
   - Grid of cards: `grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-4`
   - Each card shows: thumbnail image (80x80 or larger via thumb variant URL), title, category name, upload date formatted
   - Each card has a DropdownMenu (three-dot icon trigger, MoreHorizontal from lucide) with:
     - "Edit" action: Link to `/admin/portfolio/${item.id}/edit`
     - "Delete" action: opens a Dialog confirmation
   - Delete confirmation dialog: title "Delete Photo", description "This photo will be permanently removed from the portfolio. This cannot be undone.", destructive button "Delete Photo" (bg-destructive text-white), cancel button "Cancel"
   - On delete confirm: call deletePortfolioItem(itemId), then router.refresh()
   - Empty state: heading "No portfolio photos", body "Upload your first photo to start building the portfolio.", with Upload Photo CTA button
   - This page needs a client component wrapper for the delete dialog state and action handling

2. **Create `src/app/(private)/admin/portfolio/new/page.tsx`** ("use client"):
   - Page title: "Upload Photo" with `className="font-display text-xl font-bold text-text-primary"`
   - Step 1: Upload image using `<ImageUploader bucket="public-images" folder="portfolio" onUploadComplete={setImageId} />`
   - Step 2: After upload completes (imageId is set), show metadata form:
     - Title: `<Input>` with label "Title", required, maxLength 100
     - Description: `<textarea>` with label "Description" (optional), maxLength 500, styled with same classes as Input plus `min-h-[100px] resize-y`
     - Category: `<select>` dropdown populated from getCategories(), required
   - Form validation with react-hook-form + zod: title required min 1, categoryId required
   - Submit button: "Upload Photo" (bg-accent text-white), disabled while submitting
   - Cancel button: "Cancel Upload" (variant="outline", text-text-secondary), links back to /admin/portfolio
   - On submit: call createPortfolioItem({ title, description, categoryId, imageId }), then router.push("/admin/portfolio")
   - Show toast on success: "Photo uploaded successfully"
   - Show toast on error: "Upload failed. Check your connection and try again."

3. **Create `src/app/(private)/admin/portfolio/[id]/edit/page.tsx`** (Server Component + client form):
   - Server-side: fetch portfolio item using `getPortfolioItemById(params.id)` from src/actions/portfolio.ts (created in Plan 01)
   - If item not found, show notFound() from next/navigation
   - Page title: "Edit Photo" with same styling
   - Pre-populate form with existing title, description, categoryId
   - Show current image thumbnail (not editable -- image cannot be swapped, only metadata)
   - Same form fields as upload but with existing values as defaults
   - Submit button: "Save Changes" (bg-accent text-white)
   - Cancel button: "Discard Changes" (variant="outline"), links back to /admin/portfolio
   - On submit: call updatePortfolioItem(itemId, { title, description, categoryId }), then router.push("/admin/portfolio")
   - Show toast on success: "Changes saved"
   - Show toast on error: "Changes could not be saved. Please try again."
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -20 && npx vitest run src/__tests__/portfolio/portfolio-actions.test.ts --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - src/app/(private)/admin/portfolio/page.tsx contains `getPortfolioItems`
    - src/app/(private)/admin/portfolio/page.tsx contains `deletePortfolioItem`
    - src/app/(private)/admin/portfolio/page.tsx contains `Delete Photo`
    - src/app/(private)/admin/portfolio/page.tsx contains `No portfolio photos`
    - src/app/(private)/admin/portfolio/page.tsx contains `Upload Photo`
    - src/app/(private)/admin/portfolio/new/page.tsx contains `ImageUploader`
    - src/app/(private)/admin/portfolio/new/page.tsx contains `createPortfolioItem`
    - src/app/(private)/admin/portfolio/new/page.tsx contains `bucket="public-images"`
    - src/app/(private)/admin/portfolio/new/page.tsx contains `folder="portfolio"`
    - src/app/(private)/admin/portfolio/new/page.tsx contains `Cancel Upload`
    - src/app/(private)/admin/portfolio/[id]/edit/page.tsx contains `getPortfolioItemById`
    - src/app/(private)/admin/portfolio/[id]/edit/page.tsx contains `updatePortfolioItem`
    - src/app/(private)/admin/portfolio/[id]/edit/page.tsx contains `Save Changes`
    - src/app/(private)/admin/portfolio/[id]/edit/page.tsx contains `Discard Changes`
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>Portfolio admin pages allow listing all photos, uploading new photos with metadata, editing existing photo metadata via getPortfolioItemById, and deleting photos with confirmation</done>
</task>

<task type="auto">
  <name>Task 2: Create category management, about editor, and add admin nav link</name>
  <files>src/app/(private)/admin/categories/page.tsx, src/app/(private)/admin/about/page.tsx, src/components/layout/top-nav.tsx, src/components/layout/bottom-tab-bar.tsx</files>
  <read_first>
    - src/components/layout/top-nav.tsx (current nav links structure)
    - src/components/layout/bottom-tab-bar.tsx (current mobile nav links)
    - src/actions/categories.ts (getCategories, createCategory, updateCategory, deleteCategory)
    - src/actions/about.ts (getAboutContent, updateAboutContent)
    - src/components/upload/image-uploader.tsx (for profile photo upload)
    - src/components/ui/input.tsx (Input component)
    - src/components/ui/button.tsx (Button variants)
    - .planning/phases/02-public-portfolio/02-UI-SPEC.md (category management specs, about editor specs, copywriting)
  </read_first>
  <action>
1. **Create `src/app/(private)/admin/categories/page.tsx`** ("use client"):
   - Page title: "Categories" with `className="font-display text-xl font-bold text-text-primary"`
   - Fetch categories on mount using getCategories() (call in useEffect or use startTransition)
   - Display as a list: each row shows category name, display order number, and action buttons
   - Each row: `<div className="flex items-center justify-between py-3 border-b border-border">`
   - Inline edit: clicking category name turns it into an Input field; pressing Enter or blur saves via updateCategory()
   - Delete button: lucide Trash2 icon, `text-destructive`, with confirmation dialog: "Delete Category", "This category and all its photos will need to be recategorized. Continue?", destructive confirm button
   - On delete: call deleteCategory(categoryId), handle error (show toast if category has photos)
   - Add new category section at bottom: Input field + "Add Category" button (bg-accent text-white)
   - On add: call createCategory({ name }), clear input, refresh list
   - Show toast on success for create/delete operations

2. **Create `src/app/(private)/admin/about/page.tsx`** ("use client"):
   - Page title: "About Page" with same styling
   - Fetch about content on mount using getAboutContent()
   - Form fields:
     - Bio: `<textarea>` with label "Bio", `className` matching Input styles plus `min-h-[200px] resize-y`, maxLength 2000
     - Email: `<Input>` with label "Email", type="email"
     - Instagram URL: `<Input>` with label "Instagram URL", type="url", placeholder="https://instagram.com/..."
     - TikTok URL: `<Input>` with label "TikTok URL", type="url", placeholder="https://tiktok.com/@..."
     - Profile Photo: `<ImageUploader bucket="public-images" folder="about" onUploadComplete={setProfileImageId} />`
     - Show current profile photo thumbnail if it exists
   - Form validation with react-hook-form + zod: bio max 2000, email optional valid email, URLs optional valid URL format
   - Submit button: "Save About Page" (bg-accent text-white)
   - On submit: call updateAboutContent({ bio, email, instagramUrl, tiktokUrl, profileImageId }), show toast "About page saved" on success, "Changes could not be saved. Please try again." on error

3. **Update `src/components/layout/top-nav.tsx`**:
   - Add "Admin" link to the authenticated navigation links, pointing to `/admin/portfolio`
   - Place it after existing nav links but before the user menu
   - Use lucide Settings icon or similar
   - Style: same as other nav links (text-text-secondary hover:text-text-primary)

4. **Update `src/components/layout/bottom-tab-bar.tsx`**:
   - Add "Admin" tab to the authenticated mobile navigation
   - Use lucide Settings icon
   - Point to `/admin/portfolio`
   - Style: same as other tabs
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -20 && npx vitest run --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - src/app/(private)/admin/categories/page.tsx contains `getCategories`
    - src/app/(private)/admin/categories/page.tsx contains `createCategory`
    - src/app/(private)/admin/categories/page.tsx contains `deleteCategory`
    - src/app/(private)/admin/categories/page.tsx contains `Add Category`
    - src/app/(private)/admin/categories/page.tsx contains `Delete Category`
    - src/app/(private)/admin/about/page.tsx contains `getAboutContent`
    - src/app/(private)/admin/about/page.tsx contains `updateAboutContent`
    - src/app/(private)/admin/about/page.tsx contains `Save About Page`
    - src/app/(private)/admin/about/page.tsx contains `ImageUploader`
    - src/app/(private)/admin/about/page.tsx contains `bucket="public-images"`
    - src/app/(private)/admin/about/page.tsx contains `folder="about"`
    - src/components/layout/top-nav.tsx contains `/admin/portfolio` or `admin`
    - src/components/layout/bottom-tab-bar.tsx contains `/admin/portfolio` or `admin`
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>Category management page with inline edit and add/delete, about page editor with profile photo upload, and admin navigation links added to both desktop and mobile nav</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes
- `npx vitest run --reporter=verbose` runs all portfolio test stubs
- All admin pages are within (private) route group (auth-protected by middleware)
- Both Funnghy and boyfriend can access upload page (no role restriction beyond auth)
- Categories can be created, edited, and deleted
- About content can be edited with profile photo upload
- Navigation includes admin link for authenticated users
</verification>

<success_criteria>
- Portfolio admin lists all photos with edit/delete actions
- New photo upload uses existing ImageUploader + metadata form
- Edit form pre-populates with existing data fetched via getPortfolioItemById
- Delete shows confirmation dialog before removing photo
- Category management supports create, inline edit, delete
- About page editor saves bio, email, social links, and profile photo
- Admin link visible in nav for authenticated users only
</success_criteria>

<output>
After completion, create `.planning/phases/02-public-portfolio/02-03-SUMMARY.md`
</output>
