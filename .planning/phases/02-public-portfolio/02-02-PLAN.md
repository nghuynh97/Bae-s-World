---
phase: 02-public-portfolio
plan: 02
type: execute
wave: 2
depends_on: [02-00, 02-01]
files_modified:
  - src/components/portfolio/masonry-grid.tsx
  - src/components/portfolio/gallery-card.tsx
  - src/components/portfolio/category-filter.tsx
  - src/components/portfolio/infinite-scroll-gallery.tsx
  - src/components/portfolio/lightbox.tsx
  - src/components/portfolio/about-section.tsx
  - src/components/portfolio/photo-strip.tsx
  - src/app/(public)/page.tsx
  - src/app/(public)/about/page.tsx
autonomous: true
requirements: [PORT-01, PORT-02, PORT-03, PORT-04, PORT-06]

must_haves:
  truths:
    - "A public visitor can view a masonry grid gallery of portfolio photos without logging in"
    - "Photos display in correct left-to-right reading order across 2 columns (mobile) and 3 columns (desktop)"
    - "A visitor can click any photo to open a full-size lightbox with blurred backdrop"
    - "Lightbox supports arrow buttons, keyboard arrows, swipe on mobile, and Escape to close"
    - "A visitor can filter portfolio photos by category using pill/chip buttons"
    - "Default category filter is Modeling (the isDefault category from DB)"
    - "Infinite scroll loads 12 more photos as user approaches bottom of page"
    - "About page shows profile photo, bio, email, social links, and a photo strip"
  artifacts:
    - path: "src/components/portfolio/masonry-grid.tsx"
      provides: "CSS flex columns with JS round-robin distribution"
      contains: "flex-1 flex flex-col"
    - path: "src/components/portfolio/gallery-card.tsx"
      provides: "Photo card with hover overlay showing title and category"
      contains: "group-hover:scale-[1.02]"
    - path: "src/components/portfolio/category-filter.tsx"
      provides: "Pill/chip filter row with rose gold active state"
      contains: "bg-accent"
    - path: "src/components/portfolio/infinite-scroll-gallery.tsx"
      provides: "IntersectionObserver-based infinite scroll with masonry grid"
      contains: "useInView"
    - path: "src/components/portfolio/lightbox.tsx"
      provides: "Full-screen photo viewer on base-ui Dialog"
      contains: "backdrop-blur-md"
    - path: "src/app/(public)/page.tsx"
      provides: "Portfolio gallery page replacing placeholder"
      contains: "getPortfolioItems"
    - path: "src/app/(public)/about/page.tsx"
      provides: "About page replacing placeholder"
      contains: "getAboutContent"
  key_links:
    - from: "src/app/(public)/page.tsx"
      to: "src/actions/portfolio.ts"
      via: "server-side initial data fetch"
      pattern: "getPortfolioItems"
    - from: "src/components/portfolio/infinite-scroll-gallery.tsx"
      to: "src/actions/portfolio.ts"
      via: "client-side pagination fetch"
      pattern: "getPortfolioItems"
    - from: "src/app/(public)/about/page.tsx"
      to: "src/actions/about.ts"
      via: "server-side data fetch"
      pattern: "getAboutContent"
---

<objective>
Build the public-facing gallery and about pages -- masonry grid with infinite scroll, category filtering, full-screen lightbox, and about page with bio and photo strip.

Purpose: This is the "face" of the gift -- the first thing anyone sees. A beautiful, editorial masonry gallery with smooth browsing and an elegant about page.
Output: Complete public portfolio experience (gallery + lightbox + filters + about page), all server-rendered with client-side enhancements.
</objective>

<execution_context>
@C:/Users/ADMIN/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/ADMIN/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-public-portfolio/02-CONTEXT.md
@.planning/phases/02-public-portfolio/02-RESEARCH.md
@.planning/phases/02-public-portfolio/02-UI-SPEC.md
@.planning/phases/02-public-portfolio/02-01-SUMMARY.md

<interfaces>
<!-- Server Actions from Plan 01 that this plan consumes -->

From src/actions/portfolio.ts (created in Plan 01):
```typescript
export async function getPortfolioItems(cursor?: string, categorySlug?: string): Promise<{
  items: Array<{
    item: { id: string; title: string; description: string | null; categoryId: string; imageId: string; displayOrder: number; createdAt: Date; updatedAt: Date };
    category: { id: string; name: string; slug: string };
    image: { id: string; width: number; height: number };
    variants: Array<{ variantName: string; storagePath: string; width: number; height: number; url: string }>;
  }>;
  nextCursor: string | null;
}>;
```

From src/actions/categories.ts (created in Plan 01):
```typescript
export async function getCategories(): Promise<Array<{
  id: string; name: string; slug: string; displayOrder: number; isDefault: number;
}>>;
```

From src/actions/about.ts (created in Plan 01):
```typescript
export async function getAboutContent(): Promise<{
  id: string; bio: string; email: string | null;
  instagramUrl: string | null; tiktokUrl: string | null;
  profileImageUrl: string | null;
} | null>;
```

From src/lib/supabase/storage.ts:
```typescript
export function getPublicImageUrl(bucket: string, path: string): string;
```

From src/components/ui/dialog.tsx:
```typescript
// base-ui Dialog with DialogPrimitive.Root, Trigger, Portal, Backdrop, Popup, Close, Title, Description
// DialogOverlay uses: cn("fixed inset-0 isolate z-50 bg-black/10 ...")
// DialogContent uses: DialogPrimitive.Popup
// base-ui Dialog handles focus trapping automatically
export { Dialog, DialogClose, DialogContent, DialogOverlay, DialogPortal, DialogTrigger, DialogTitle, DialogDescription };
```

From src/components/ui/skeleton.tsx:
```typescript
// Available for loading states
export { Skeleton };
```

From src/app/globals.css (design tokens):
```css
--color-dominant: #F8F6FF;
--color-surface: #FFFFFF;
--color-accent: #E8B4B8;
--color-text-primary: #2D2235;
--color-text-secondary: #6B5F76;
--color-border: #E8E4EE;
--font-display: 'Playfair Display', serif;
--font-body: 'Inter', sans-serif;
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create gallery components (masonry grid, gallery card, category filter, infinite scroll)</name>
  <files>src/components/portfolio/masonry-grid.tsx, src/components/portfolio/gallery-card.tsx, src/components/portfolio/category-filter.tsx, src/components/portfolio/infinite-scroll-gallery.tsx, src/app/(public)/page.tsx</files>
  <read_first>
    - src/app/(public)/page.tsx (current placeholder to replace)
    - src/app/(public)/layout.tsx (parent layout structure, padding, max-width)
    - src/app/globals.css (design tokens)
    - src/actions/portfolio.ts (getPortfolioItems signature and return type)
    - src/actions/categories.ts (getCategories signature and return type)
    - src/components/ui/skeleton.tsx (loading state component)
    - .planning/phases/02-public-portfolio/02-UI-SPEC.md (interaction contracts, spacing, typography, colors)
    - .planning/phases/02-public-portfolio/02-RESEARCH.md (Pattern 1: masonry, Pattern 2: infinite scroll, pitfalls)
  </read_first>
  <action>
1. **Create `src/components/portfolio/masonry-grid.tsx`** ("use client"):
   - Props: `{ items: React.ReactNode[]; columns?: number }`
   - Use round-robin distribution: `cols[index % columns].push(item)` for correct L-to-R reading order
   - Render as `<div className="flex gap-4">` with each column as `<div className="flex-1 flex flex-col gap-4">`
   - Use useMemo for column distribution, keyed on items and columns

2. **Create `src/components/portfolio/gallery-card.tsx`** ("use client"):
   - Props: `{ title: string; category: string; imageUrl: string; width: number; height: number; onClick: () => void }`
   - Render as `<button>` with `className="group relative w-full overflow-hidden rounded-lg cursor-pointer"`
   - Set `style={{ aspectRatio: \`${width}/${height}\` }}` to preserve natural aspect ratio
   - Image: `<img src={imageUrl} alt={title} loading="lazy" className="w-full h-full object-cover transition-transform duration-300 group-hover:scale-[1.02]" />`
   - Hover overlay: `<div className="absolute inset-0 bg-black/0 group-hover:bg-black/30 transition-colors duration-300 flex items-end p-4">`
   - Inside overlay: title at 14px bold white, category at 12px normal white/80, both with `opacity-0 group-hover:opacity-100 transition-opacity duration-300`
   - Respect prefers-reduced-motion: wrap scale transform in `motion-safe:` prefix

3. **Create `src/components/portfolio/category-filter.tsx`** ("use client"):
   - Props: `{ categories: Array<{ id: string; name: string; slug: string }>; activeSlug: string; onSelect: (slug: string) => void }`
   - Prepend `{ id: "all", name: "All", slug: "all" }` to the categories array
   - Render horizontal scrollable row: `<div className="flex gap-2 overflow-x-auto pb-2 scrollbar-hide">`
   - Each pill: `<button>` with `className="px-4 py-2 rounded-full text-sm font-body whitespace-nowrap transition-colors"`
   - Active pill: `bg-accent text-white`
   - Inactive pill: `border border-border text-text-secondary hover:border-accent hover:text-accent`
   - Add `role="tablist"` on container, `role="tab"` and `aria-selected` on each pill

4. **Create `src/components/portfolio/infinite-scroll-gallery.tsx`** ("use client"):
   - Props: `{ initialItems: PortfolioItem[]; initialCursor: string | null; initialCategorySlug: string; categories: Category[] }`
   - State: items, cursor, activeCategorySlug
   - Use `useInView` from react-intersection-observer with `{ threshold: 0, rootMargin: "200px" }`
   - Use `useTransition` isPending as guard against double-fetching
   - When inView and cursor exists and not isPending, call `getPortfolioItems(cursor, activeCategorySlug)` and append results
   - When category changes: reset items to empty, set cursor to null, fetch fresh page with new category slug
   - Render: CategoryFilter on top, MasonryGrid below (2 columns mobile via responsive hook, 3 desktop), sentinel div at bottom, loading skeletons when pending
   - Use a simple responsive hook: `const [columns, setColumns] = useState(3)` with `useEffect` checking `window.matchMedia("(max-width: 768px)")` and its change listener; return 2 for mobile, 3 for desktop
   - Empty state when items.length === 0 and not pending: heading "No photos yet" (font-display text-xl), body "Your portfolio is waiting for its first photo. Head to the admin panel to upload one." (text-text-secondary text-sm)
   - Loading skeletons: 3 Skeleton components matching gallery card aspect ratios
   - Add 200ms opacity transition on gallery items when switching categories: wrap MasonryGrid in div with `transition-opacity duration-200` and `opacity-0` while pending category switch, `opacity-100` otherwise

5. **Replace `src/app/(public)/page.tsx`** (Server Component):
   - Import and call `getCategories()` server-side
   - Find the default category (isDefault === 1) or fall back to first category
   - Import and call `getPortfolioItems(undefined, defaultCategorySlug)` for initial server-rendered data
   - Render page title "Portfolio" with `className="font-display text-2xl font-bold text-text-primary mb-6 pt-8"`
   - Render `<InfiniteScrollGallery initialItems={items} initialCursor={nextCursor} initialCategorySlug={defaultSlug} categories={categories} />`
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -20 && npx vitest run src/__tests__/portfolio/masonry-grid.test.tsx src/__tests__/portfolio/category-filter.test.tsx --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - src/components/portfolio/masonry-grid.tsx contains `"use client"` and `flex-1 flex flex-col gap-4`
    - src/components/portfolio/masonry-grid.tsx contains `index % columns`
    - src/components/portfolio/gallery-card.tsx contains `group-hover:scale-[1.02]`
    - src/components/portfolio/gallery-card.tsx contains `bg-black/30`
    - src/components/portfolio/gallery-card.tsx contains `aspectRatio`
    - src/components/portfolio/category-filter.tsx contains `bg-accent text-white`
    - src/components/portfolio/category-filter.tsx contains `role="tablist"`
    - src/components/portfolio/category-filter.tsx contains `aria-selected`
    - src/components/portfolio/infinite-scroll-gallery.tsx contains `useInView`
    - src/components/portfolio/infinite-scroll-gallery.tsx contains `useTransition`
    - src/components/portfolio/infinite-scroll-gallery.tsx contains `getPortfolioItems`
    - src/components/portfolio/infinite-scroll-gallery.tsx contains `rootMargin`
    - src/components/portfolio/infinite-scroll-gallery.tsx contains `No photos yet`
    - src/app/(public)/page.tsx contains `getPortfolioItems`
    - src/app/(public)/page.tsx contains `getCategories`
    - src/app/(public)/page.tsx contains `InfiniteScrollGallery`
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>Masonry gallery with category filtering, infinite scroll, and hover effects renders on the public portfolio page; default category is Modeling; 12 items per page with cursor pagination</done>
</task>

<task type="auto">
  <name>Task 2: Create lightbox and about page</name>
  <files>src/components/portfolio/lightbox.tsx, src/components/portfolio/about-section.tsx, src/components/portfolio/photo-strip.tsx, src/app/(public)/about/page.tsx</files>
  <read_first>
    - src/components/ui/dialog.tsx (base-ui Dialog primitives, DialogOverlay, DialogContent structure)
    - src/components/portfolio/infinite-scroll-gallery.tsx (just created -- needs to integrate lightbox)
    - src/app/(public)/about/page.tsx (current placeholder to replace)
    - src/actions/about.ts (getAboutContent signature)
    - src/actions/portfolio.ts (getPortfolioItems for photo strip)
    - src/app/globals.css (design tokens)
    - .planning/phases/02-public-portfolio/02-UI-SPEC.md (lightbox interaction contract, about page layout, photo strip specs)
    - .planning/phases/02-public-portfolio/02-RESEARCH.md (Pattern 3: lightbox, Pitfall 3: body scroll, Pitfall 6: swipe conflicts)
  </read_first>
  <action>
1. **Create `src/components/portfolio/lightbox.tsx`** ("use client"):
   - Props: `{ items: PortfolioItem[]; currentIndex: number; open: boolean; onOpenChange: (open: boolean) => void; onNavigate: (index: number) => void }`
   - Build on base-ui Dialog primitives directly (NOT the shadcn wrapper -- the lightbox needs custom layout):
     ```typescript
     import { Dialog as DialogPrimitive } from "@base-ui/react/dialog";
     ```
   - Structure:
     - `<DialogPrimitive.Root open={open} onOpenChange={onOpenChange}>`
     - `<DialogPrimitive.Portal>`
     - Backdrop: `<DialogPrimitive.Backdrop className="fixed inset-0 z-50 bg-black/40 backdrop-blur-md" />`
     - Popup: `<DialogPrimitive.Popup className="fixed inset-0 z-50 flex items-center justify-center outline-none">`
   - Close button: top-right, lucide X icon, 24px, white text, `aria-label="Close lightbox"`, 44px min touch target (w-11 h-11 rounded-full bg-black/20 flex items-center justify-center)
   - Navigation arrows: ChevronLeft / ChevronRight from lucide-react, centered vertically on left/right edges, same 44px touch target style as close button, `aria-label="Previous photo"` / `aria-label="Next photo"`
   - Hide prev arrow when currentIndex === 0, hide next arrow when currentIndex === items.length - 1
   - Photo: `<img>` centered, `className="max-w-[90vw] max-h-[80vh] object-contain rounded-lg"`
   - Use the "large" variant URL if available, fall back to "full" variant
   - Below photo: white text panel with title (font-display text-xl font-bold text-white), category (text-sm text-white/80), description if present (text-sm text-white/70 mt-1)
   - **Keyboard navigation:** useEffect with keydown listener: ArrowLeft calls goToPrev, ArrowRight calls goToNext, Escape calls onOpenChange(false). Add cleanup. Wrap goToPrev/goToNext in useCallback.
   - **Swipe support:** Track touchstart/touchend X and Y coordinates. On touchend, if |deltaX| > 50 && |deltaX| > |deltaY|: negative deltaX = next, positive = prev. Use refs for touch tracking.
   - **Body scroll lock:** useEffect that sets `document.body.style.overflow = 'hidden'` when open, restores to '' on close/unmount.
   - **Lightbox photo transition:** Wrap img in a div with `key={currentIndex}` and CSS `animate-in fade-in-0 duration-200` for crossfade effect on navigation.
   - Click on backdrop (outside photo area) closes via Dialog's built-in behavior.

2. **Integrate lightbox into infinite-scroll-gallery.tsx:**
   - Add state: `const [lightboxIndex, setLightboxIndex] = useState<number | null>(null)`
   - Pass `onClick={() => setLightboxIndex(globalIndex)}` to each GalleryCard (where globalIndex is the item's position in the flat items array)
   - Render `<Lightbox items={items} currentIndex={lightboxIndex ?? 0} open={lightboxIndex !== null} onOpenChange={(open) => !open && setLightboxIndex(null)} onNavigate={setLightboxIndex} />` at the bottom of the component

3. **Create `src/components/portfolio/about-section.tsx`** (Server Component):
   - Props: `{ bio: string; email: string | null; instagramUrl: string | null; tiktokUrl: string | null; profileImageUrl: string | null }`
   - Desktop layout: `grid grid-cols-1 md:grid-cols-[40%_1fr] gap-12`
   - Left column: profile photo with `<img>` tag, `className="w-full rounded-lg object-cover shadow-md max-h-[400px]"`, alt="Funnghy"
   - If no profile image, show a placeholder div with soft gradient background and camera icon
   - Right column: bio with `className="text-base font-body leading-relaxed text-text-primary"`
   - Contact section below bio: email as `<a href="mailto:${email}">` with Mail icon, social links as icon links (Instagram icon for instagramUrl, lucide icons)
   - Social icons: 24px lucide icons with 8px gap, `text-text-secondary hover:text-accent transition-colors`
   - Empty state if bio is empty: "About page not set up yet" heading, "Check back soon." body

4. **Create `src/components/portfolio/photo-strip.tsx`** (Server Component):
   - Props: `{ items: Array<{ imageUrl: string; title: string }> }`
   - Render 3-4 thumbnails horizontally: `<div className="flex gap-2 mt-8">`
   - Each thumbnail: `<a href="/">` (links to gallery), `<img>` at 80x80, `className="w-20 h-20 object-cover rounded-md"`, alt={title} or alt="" for decorative
   - If no items, render nothing

5. **Replace `src/app/(public)/about/page.tsx`** (Server Component):
   - Import and call `getAboutContent()` server-side
   - Import and call `getPortfolioItems(undefined, undefined)` with limit awareness (only need first 4 for photo strip) -- or just take first 4 from the returned items
   - Render page title "About Funnghy" with `className="font-display text-2xl font-bold text-text-primary mb-6 pt-8"`
   - Render `<AboutSection>` with about content props
   - Render `<Separator className="my-8" />` then `<PhotoStrip>` with the 4 most recent portfolio item thumbnail URLs
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -20 && npx vitest run src/__tests__/portfolio/lightbox.test.tsx src/__tests__/portfolio/about-section.test.tsx --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - src/components/portfolio/lightbox.tsx contains `"use client"`
    - src/components/portfolio/lightbox.tsx contains `backdrop-blur-md`
    - src/components/portfolio/lightbox.tsx contains `DialogPrimitive` (uses base-ui directly)
    - src/components/portfolio/lightbox.tsx contains `ArrowLeft` or `ChevronLeft`
    - src/components/portfolio/lightbox.tsx contains `aria-label="Close lightbox"`
    - src/components/portfolio/lightbox.tsx contains `aria-label="Previous photo"`
    - src/components/portfolio/lightbox.tsx contains `aria-label="Next photo"`
    - src/components/portfolio/lightbox.tsx contains `touchstart` or `onTouchStart`
    - src/components/portfolio/lightbox.tsx contains `document.body.style.overflow`
    - src/components/portfolio/lightbox.tsx contains `max-w-[90vw]`
    - src/components/portfolio/infinite-scroll-gallery.tsx contains `Lightbox`
    - src/components/portfolio/infinite-scroll-gallery.tsx contains `lightboxIndex`
    - src/components/portfolio/about-section.tsx contains `grid-cols-[40%_1fr]` or `grid-cols-1 md:grid-cols`
    - src/components/portfolio/about-section.tsx contains `mailto:`
    - src/components/portfolio/photo-strip.tsx contains `w-20 h-20`
    - src/app/(public)/about/page.tsx contains `getAboutContent`
    - src/app/(public)/about/page.tsx contains `AboutSection`
    - src/app/(public)/about/page.tsx contains `About Funnghy`
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>Lightbox opens from gallery cards with keyboard/swipe/arrow navigation and blurred backdrop; about page shows bio, contact info, social links, and photo strip; all public pages render without auth</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes
- `npx vitest run --reporter=verbose` runs all portfolio test stubs (todo tests reported, no failures)
- Portfolio page imports and renders InfiniteScrollGallery with server-fetched initial data
- About page imports and renders AboutSection with server-fetched content
- Lightbox integrated into gallery with open/close/navigate functionality
- All components use correct design tokens from UI-SPEC
- No auth required for public pages (PORT-06)
</verification>

<success_criteria>
- Public portfolio page shows masonry grid with category filter pills (default: Modeling)
- Clicking a photo opens full-screen lightbox with blurred backdrop, arrows, keyboard nav, swipe
- Infinite scroll loads 12 more items per page using cursor pagination
- About page shows profile photo, bio, email, social links, and photo strip
- All pages publicly accessible without login
- Empty states shown when no content exists
</success_criteria>

<output>
After completion, create `.planning/phases/02-public-portfolio/02-02-SUMMARY.md`
</output>
