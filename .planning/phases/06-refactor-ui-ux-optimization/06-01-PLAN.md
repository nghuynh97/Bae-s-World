---
phase: 06-refactor-ui-ux-optimization
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/ui/select.tsx
  - src/components/ui/textarea.tsx
  - src/components/beauty/product-form.tsx
  - src/components/beauty/routine-step-search.tsx
  - src/app/(private)/admin/portfolio/new/page.tsx
  - src/app/(private)/admin/portfolio/[id]/edit/edit-form.tsx
  - src/actions/schedule.ts
  - src/actions/beauty-products.ts
  - src/actions/beauty-categories.ts
  - src/actions/routines.ts
  - src/actions/portfolio.ts
  - src/actions/about.ts
  - src/components/schedule/job-form.tsx
  - src/components/beauty/routine-list.tsx
  - src/components/beauty/product-grid.tsx
  - src/app/(private)/admin/portfolio/portfolio-list-client.tsx
  - src/app/(private)/admin/about/page.tsx
  - src/components/schedule/job-form.tsx
autonomous: true
requirements:
  - REFAC-02
  - REFAC-03
  - REFAC-04
  - REFAC-05

must_haves:
  truths:
    - "All dropdown selects in forms render as styled shadcn Select components (not native browser selects)"
    - "All textareas render as shadcn Textarea components with consistent design system styling"
    - "After any CRUD action (create/edit/delete), the UI updates without manual page refresh"
    - "No router.refresh() calls remain in client components"
    - "All revalidatePath() calls exist in server actions after mutations"
  artifacts:
    - path: "src/components/ui/select.tsx"
      provides: "shadcn Select component"
    - path: "src/components/ui/textarea.tsx"
      provides: "shadcn Textarea component"
    - path: "src/actions/schedule.ts"
      provides: "revalidatePath('/schedule') after mutations"
      contains: "revalidatePath"
    - path: "src/actions/beauty-products.ts"
      provides: "revalidatePath('/beauty') after mutations"
      contains: "revalidatePath"
    - path: "src/actions/portfolio.ts"
      provides: "revalidatePath after portfolio mutations"
      contains: "revalidatePath"
  key_links:
    - from: "src/components/beauty/product-form.tsx"
      to: "src/components/ui/select.tsx"
      via: "Controller + Select for categoryId"
      pattern: "Controller.*name.*categoryId"
    - from: "src/actions/schedule.ts"
      to: "next/cache"
      via: "revalidatePath import"
      pattern: "import.*revalidatePath.*from.*next/cache"
---

<objective>
Replace all native HTML form elements with shadcn components and migrate router.refresh() to revalidatePath() in server actions.

Purpose: Achieve visual consistency across all forms (shadcn design system) and eliminate page-flash on CRUD operations by using server-side revalidation.
Output: All forms use shadcn Select/Textarea/Input, all server actions use revalidatePath(), no router.refresh() remains in client code.
</objective>

<execution_context>
@C:/Users/ADMIN/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/ADMIN/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-refactor-ui-ux-optimization/06-RESEARCH.md

<interfaces>
<!-- shadcn component patterns already in use -->

From src/components/ui/input.tsx (existing pattern to follow):
- Thin wrapper around native <input> with forwardRef
- Works with register() from react-hook-form

From src/components/beauty/product-form.tsx (current native select to replace):
- Line 212: <select {...register('categoryId')} ...>
- Line 242: <textarea {...register('notes')} ...>
- Uses react-hook-form with register() pattern

From src/actions/schedule.ts (current pattern, needs revalidatePath):
- export async function createJob(...)
- export async function updateJob(...)
- export async function deleteJob(...)
- No revalidatePath calls currently

From src/components/schedule/job-form.tsx (router.refresh to remove):
- Line 134: router.refresh() after create
- Line 149: router.refresh() after update
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install shadcn Select + Textarea and replace all native form elements</name>
  <files>
    src/components/ui/select.tsx,
    src/components/ui/textarea.tsx,
    src/components/beauty/product-form.tsx,
    src/components/beauty/routine-step-search.tsx,
    src/app/(private)/admin/portfolio/new/page.tsx,
    src/app/(private)/admin/portfolio/[id]/edit/edit-form.tsx,
    src/app/(private)/admin/about/page.tsx,
    src/components/schedule/job-form.tsx
  </files>
  <read_first>
    src/components/beauty/product-form.tsx,
    src/app/(private)/admin/portfolio/new/page.tsx,
    src/app/(private)/admin/portfolio/[id]/edit/edit-form.tsx,
    src/components/beauty/routine-step-search.tsx,
    src/app/(private)/admin/about/page.tsx,
    src/components/schedule/job-form.tsx
  </read_first>
  <action>
1. Install shadcn Select and Textarea:
   ```bash
   npx shadcn@latest add select textarea
   ```

2. Replace native `<select>` with shadcn Select + Controller in 3 files:
   - `src/components/beauty/product-form.tsx` line 212: categoryId select
   - `src/app/(private)/admin/portfolio/new/page.tsx` line 116: categoryId select
   - `src/app/(private)/admin/portfolio/[id]/edit/edit-form.tsx` line 102: categoryId select

   For each file:
   - Add import: `import { Controller } from 'react-hook-form';`
   - Add import: `import { Select, SelectContent, SelectItem, SelectTrigger, SelectValue } from '@/components/ui/select';`
   - Replace `<select {...register('categoryId')} ...>` with Controller + Select pattern:
     ```tsx
     <Controller
       control={control}
       name="categoryId"
       render={({ field }) => (
         <Select onValueChange={field.onChange} value={field.value}>
           <SelectTrigger className="w-full">
             <SelectValue placeholder="Select a category" />
           </SelectTrigger>
           <SelectContent>
             {categories.map((cat) => (
               <SelectItem key={cat.id} value={cat.id}>
                 {cat.name}
               </SelectItem>
             ))}
           </SelectContent>
         </Select>
       )}
     />
     ```
   - CRITICAL: shadcn Select does NOT work with register() -- must use Controller

3. Replace native `<textarea>` with shadcn Textarea in 5 files:
   - `src/components/beauty/product-form.tsx` line 242: notes field
   - `src/components/schedule/job-form.tsx` line 313: notes field
   - `src/app/(private)/admin/about/page.tsx` line 124: bio field
   - `src/app/(private)/admin/portfolio/new/page.tsx` line 101: description field
   - `src/app/(private)/admin/portfolio/[id]/edit/edit-form.tsx` line 87: description field

   For each: Add `import { Textarea } from '@/components/ui/textarea';` and replace `<textarea>` with `<Textarea>`. register() still works with Textarea (it wraps native textarea).

4. Replace native `<input>` in routine-step-search:
   - `src/components/beauty/routine-step-search.tsx` line 102: search input
   - Replace with shadcn `Input` from `@/components/ui/input` (already installed)
   - Keep the same props, just swap the element

5. Keep hidden `<input>` from react-dropzone in image-uploader.tsx (per user decision -- do NOT touch).
  </action>
  <verify>
    <automated>cd D:/Learn/TestGSD && npx vitest run --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - grep -r "<select" src/components/ src/app/ returns zero matches (except react-dropzone hidden input)
    - grep -r "<textarea" src/components/ src/app/ returns zero matches
    - grep -r "from '@/components/ui/select'" src/ returns 3+ matches
    - grep -r "from '@/components/ui/textarea'" src/ returns 5+ matches
    - grep -r "Controller" src/components/beauty/product-form.tsx returns at least 1 match
    - src/components/ui/select.tsx exists
    - src/components/ui/textarea.tsx exists
  </acceptance_criteria>
  <done>All native select, textarea, and search input elements replaced with shadcn equivalents. No native form elements remain (except react-dropzone hidden file input).</done>
</task>

<task type="auto">
  <name>Task 2: Migrate router.refresh() to revalidatePath() in all server actions</name>
  <files>
    src/actions/schedule.ts,
    src/actions/beauty-products.ts,
    src/actions/beauty-categories.ts,
    src/actions/routines.ts,
    src/actions/portfolio.ts,
    src/actions/about.ts,
    src/components/schedule/job-form.tsx,
    src/components/beauty/routine-list.tsx,
    src/components/beauty/product-grid.tsx,
    src/app/(private)/admin/portfolio/portfolio-list-client.tsx
  </files>
  <read_first>
    src/actions/schedule.ts,
    src/actions/beauty-products.ts,
    src/actions/beauty-categories.ts,
    src/actions/routines.ts,
    src/actions/portfolio.ts,
    src/actions/about.ts,
    src/components/schedule/job-form.tsx,
    src/components/beauty/routine-list.tsx,
    src/components/beauty/product-grid.tsx,
    src/app/(private)/admin/portfolio/portfolio-list-client.tsx
  </read_first>
  <action>
1. Add revalidatePath to ALL mutating server actions. Import `import { revalidatePath } from 'next/cache';` in each file. Add revalidatePath call AFTER the DB mutation, BEFORE any return/redirect. IMPORTANT: if redirect() is used, call it OUTSIDE try/catch blocks (Next.js redirect throws internally).

   Specific changes per file:

   **src/actions/schedule.ts** -- add `revalidatePath('/schedule')` after mutations in:
   - createJob (after insert)
   - updateJob (after update)
   - deleteJob (after delete)

   **src/actions/beauty-products.ts** -- add `revalidatePath('/beauty')` after mutations in:
   - createBeautyProduct
   - updateBeautyProduct
   - deleteBeautyProduct
   - toggleFavorite

   **src/actions/beauty-categories.ts** -- add `revalidatePath('/beauty')` after mutations in:
   - all create/rename/delete category functions

   **src/actions/routines.ts** -- add `revalidatePath('/beauty')` after mutations in:
   - addStep, removeStep, reorderSteps (all routine mutation functions)

   **src/actions/portfolio.ts** -- add `revalidatePath('/admin/portfolio')` AND `revalidatePath('/')` after mutations in:
   - createPortfolioItem, updatePortfolioItem, deletePortfolioItem
   - Two paths because portfolio shows on public home page AND admin page

   **src/actions/about.ts** -- add `revalidatePath('/about')` AND `revalidatePath('/admin/about')` after mutations in:
   - updateAboutContent

2. Remove ALL router.refresh() calls from client components:

   **src/components/schedule/job-form.tsx** lines 134, 149:
   - Delete `router.refresh();` lines
   - If `useRouter` is no longer used for anything else, remove the import and hook call too

   **src/components/beauty/routine-list.tsx** line 117:
   - Delete `router.refresh();`
   - Check if router is still needed (for navigation etc), remove if not

   **src/components/beauty/product-grid.tsx** lines 132, 232:
   - Delete `router.refresh();` on line 132
   - Delete the `onCategoriesChanged={() => router.refresh()}` callback on line 232
   - Remove the onCategoriesChanged prop entirely if it was only used for router.refresh()

   **src/app/(private)/admin/portfolio/portfolio-list-client.tsx** line 56:
   - Delete `router.refresh();`
   - Clean up useRouter if no longer needed

3. For forms that navigate after submit (e.g., portfolio create redirects to list), ensure the server action calls revalidatePath() then redirect() -- with redirect() OUTSIDE any try/catch block.
  </action>
  <verify>
    <automated>cd D:/Learn/TestGSD && npx vitest run --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - grep -r "router\.refresh()" src/ returns zero matches
    - grep -r "revalidatePath" src/actions/schedule.ts returns at least 3 matches
    - grep -r "revalidatePath" src/actions/beauty-products.ts returns at least 4 matches
    - grep -r "revalidatePath" src/actions/beauty-categories.ts returns at least 1 match
    - grep -r "revalidatePath" src/actions/routines.ts returns at least 1 match
    - grep -r "revalidatePath" src/actions/portfolio.ts returns at least 3 matches
    - grep -r "revalidatePath" src/actions/about.ts returns at least 1 match
    - No redirect() calls inside try/catch blocks in server actions
  </acceptance_criteria>
  <done>All server actions call revalidatePath() after mutations. All router.refresh() calls removed from client components. UI updates seamlessly after CRUD operations without page flash.</done>
</task>

</tasks>

<verification>
1. `npx vitest run` -- all existing tests pass (regression check)
2. `grep -r "<select" src/ --include="*.tsx"` -- zero native selects (except hidden file input)
3. `grep -r "<textarea" src/ --include="*.tsx"` -- zero native textareas
4. `grep -r "router.refresh()" src/` -- zero matches
5. `grep -r "revalidatePath" src/actions/` -- matches in all 6 action files
</verification>

<success_criteria>
- All form elements use shadcn components (Select, Textarea, Input)
- All server actions include revalidatePath() after mutations
- No router.refresh() calls remain in client code
- All existing tests continue to pass
</success_criteria>

<output>
After completion, create `.planning/phases/06-refactor-ui-ux-optimization/06-01-SUMMARY.md`
</output>
