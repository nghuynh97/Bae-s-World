---
phase: 06-refactor-ui-ux-optimization
plan: 03
type: execute
wave: 2
depends_on:
  - 06-01
  - 06-02
files_modified:
  - .prettierrc
  - .prettierignore
  - .vscode/settings.json
  - package.json
autonomous: true
requirements:
  - REFAC-01

must_haves:
  truths:
    - "Prettier is installed and configured with the exact user-specified settings"
    - "Running npm run format formats the entire codebase without errors"
    - "Running npm run format:check passes with zero violations after formatting"
    - "Tailwind classes are automatically sorted by prettier-plugin-tailwindcss"
    - "VS Code formatOnSave is configured for the project"
  artifacts:
    - path: ".prettierrc"
      provides: "Prettier configuration"
      contains: "singleQuote"
    - path: ".prettierignore"
      provides: "Files excluded from formatting"
      contains: "node_modules"
    - path: ".vscode/settings.json"
      provides: "VS Code format-on-save settings"
      contains: "formatOnSave"
    - path: "package.json"
      provides: "format and format:check npm scripts"
      contains: "prettier --write"
  key_links:
    - from: ".prettierrc"
      to: "src/app/globals.css"
      via: "tailwindStylesheet option pointing to CSS config"
      pattern: "tailwindStylesheet.*globals.css"
---

<objective>
Install Prettier with tailwindcss plugin, configure per user specifications, and format the entire codebase.

Purpose: Establish consistent code formatting across the project with automated Tailwind class sorting. This MUST run last (after all component and logic changes in plans 01 and 02) to avoid noisy diffs that obscure real changes.
Output: Prettier configured and entire codebase formatted. All future code automatically formatted on save.
</objective>

<execution_context>
@C:/Users/ADMIN/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/ADMIN/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-refactor-ui-ux-optimization/06-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install Prettier, create config files, and format codebase</name>
  <files>
    .prettierrc,
    .prettierignore,
    .vscode/settings.json,
    package.json
  </files>
  <read_first>
    package.json,
    .vscode/settings.json
  </read_first>
  <action>
1. Install Prettier and Tailwind plugin as devDependencies:
   ```bash
   npm install -D prettier prettier-plugin-tailwindcss
   ```

2. Create `.prettierrc` with EXACT user-specified settings:
   ```json
   {
     "semi": true,
     "singleQuote": true,
     "trailingComma": "all",
     "tabWidth": 2,
     "printWidth": 80,
     "plugins": ["prettier-plugin-tailwindcss"],
     "tailwindStylesheet": "./src/app/globals.css"
   }
   ```
   NOTE: `tailwindStylesheet` is REQUIRED for Tailwind v4 (CSS-based config). Without it, class sorting will not work.

3. Create `.prettierignore`:
   ```
   node_modules
   .next
   dist
   pnpm-lock.yaml
   package-lock.json
   ```

4. Add npm scripts to `package.json` (in the "scripts" section):
   - `"format": "prettier --write ."`
   - `"format:check": "prettier --check ."`

5. Create or update `.vscode/settings.json`:
   - If file exists, merge these settings (preserve existing settings)
   - If file does not exist, create it
   ```json
   {
     "editor.formatOnSave": true,
     "editor.defaultFormatter": "esbenp.prettier-vscode"
   }
   ```

6. Run `npm run format` to format the ENTIRE codebase in one pass.

7. Verify with `npm run format:check` -- should pass with zero violations.

8. Run `npx vitest run` to confirm tests still pass after formatting.
  </action>
  <verify>
    <automated>cd D:/Learn/TestGSD && npm run format:check 2>&1 | tail -5 && npx vitest run --reporter=verbose 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - .prettierrc exists and contains "singleQuote": true
    - .prettierrc contains "tailwindStylesheet": "./src/app/globals.css"
    - .prettierignore exists and contains "node_modules"
    - grep "format" package.json returns matches for both format and format:check scripts
    - .vscode/settings.json exists and contains "formatOnSave": true
    - npm run format:check exits with code 0 (all files formatted)
    - npx vitest run passes (no test regressions from formatting)
  </acceptance_criteria>
  <done>Prettier installed and configured with single quotes, semicolons, trailing commas, 80 char width, and Tailwind class sorting. Entire codebase formatted. VS Code format-on-save configured. All tests pass.</done>
</task>

</tasks>

<verification>
1. `npm run format:check` -- exits cleanly with zero violations
2. `npx vitest run` -- all tests pass after formatting
3. `.prettierrc` matches exact user specification
4. `npm run format` is idempotent (running twice produces no changes)
</verification>

<success_criteria>
- Prettier 3.8.x installed with prettier-plugin-tailwindcss
- .prettierrc has exact settings from user decisions
- npm run format:check passes
- All tests pass after formatting
- VS Code settings configured for format-on-save
</success_criteria>

<output>
After completion, create `.planning/phases/06-refactor-ui-ux-optimization/06-03-SUMMARY.md`
</output>
