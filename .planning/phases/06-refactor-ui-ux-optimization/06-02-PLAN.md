---
phase: 06-refactor-ui-ux-optimization
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/schedule/job-form.tsx
  - src/components/schedule/job-card.tsx
  - src/components/schedule/calendar-grid.tsx
  - src/components/schedule/day-cell.tsx
  - src/components/schedule/stats-header.tsx
  - src/components/beauty/product-form.tsx
autonomous: true
requirements:
  - REFAC-06
  - REFAC-07
  - REFAC-08
  - REFAC-09

must_haves:
  truths:
    - "Forms have generous spacing between fields with clear visual hierarchy"
    - "Calendar day cells have visible borders, clear today highlight, and readable income amounts"
    - "Cards (job, product, stat) have enough contrast to stand out against the lavender background"
    - "All components use design system tokens consistently (accent, surface, text-primary, text-secondary)"
  artifacts:
    - path: "src/components/schedule/day-cell.tsx"
      provides: "Improved calendar day cell with borders, today highlight, job dot styling"
    - path: "src/components/schedule/job-card.tsx"
      provides: "Card with stronger shadow/ring for contrast"
    - path: "src/components/schedule/stats-header.tsx"
      provides: "Stat cards with improved contrast"
    - path: "src/components/beauty/product-form.tsx"
      provides: "Form with proper spacing scale"
  key_links:
    - from: "src/components/schedule/day-cell.tsx"
      to: "src/app/globals.css"
      via: "design system color tokens"
      pattern: "var\\(--"
    - from: "src/components/schedule/job-card.tsx"
      to: "src/app/globals.css"
      via: "shadow and ring utilities"
      pattern: "shadow|ring"
---

<objective>
Improve UX spacing, calendar visuals, and card contrast across schedule and beauty components.

Purpose: Make forms feel spacious and elegant, calendar feel polished, and cards pop against the lavender background -- achieving the premium aesthetic the design system intends.
Output: All schedule and beauty components updated with consistent spacing scale, improved calendar treatment, and proper card contrast.
</objective>

<execution_context>
@C:/Users/ADMIN/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/ADMIN/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-refactor-ui-ux-optimization/06-CONTEXT.md
@src/app/globals.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Improve form spacing and card contrast in schedule components</name>
  <files>
    src/components/schedule/job-form.tsx,
    src/components/schedule/job-card.tsx,
    src/components/schedule/stats-header.tsx
  </files>
  <read_first>
    src/components/schedule/job-form.tsx,
    src/components/schedule/job-card.tsx,
    src/components/schedule/stats-header.tsx,
    src/app/globals.css
  </read_first>
  <action>
1. **job-form.tsx** -- Fix cramped form spacing:
   - Change form container padding to `p-6` (24px) if currently less
   - Change gap between form fields to `gap-4` (16px) minimum, prefer `gap-5` or `gap-6` for visual breathing room
   - Add `gap-2` (8px) between labels and their inputs
   - Add `gap-6` (24px) between form sections (e.g., between field groups and submit button)
   - Ensure consistent use of Label component for all field labels

2. **job-card.tsx** -- Improve card contrast against lavender background:
   - Add `shadow-sm` base shadow (or strengthen existing shadow)
   - Add `ring-1 ring-black/5` for subtle border definition OR use `border border-border/50`
   - Add `bg-white` (or `bg-card`) explicitly to ensure card surface contrast
   - Ensure hover state uses `shadow-md` for lift effect (should already exist from Phase 5 polish)
   - Use design system text tokens: `text-foreground` for primary text, `text-muted-foreground` for secondary

3. **stats-header.tsx** -- Improve stat card contrast:
   - Add `shadow-sm` to stat cards
   - Add `ring-1 ring-black/5` or `border border-border/50` for definition
   - Ensure `bg-white` or `bg-card` background explicitly set
   - Use consistent padding `p-4` (16px) inside stat cards
   - Ensure amounts use `text-foreground` and labels use `text-muted-foreground`
  </action>
  <verify>
    <automated>cd D:/Learn/TestGSD && npx vitest run --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - grep "gap-4\|gap-5\|gap-6" src/components/schedule/job-form.tsx returns matches (spacing applied)
    - grep "shadow" src/components/schedule/job-card.tsx returns matches (card shadow applied)
    - grep "shadow" src/components/schedule/stats-header.tsx returns matches (stat card shadow applied)
    - grep "p-6\|p-4" src/components/schedule/job-form.tsx returns matches (container padding)
  </acceptance_criteria>
  <done>Schedule forms have generous field spacing. Job cards and stat cards have visible shadow/ring contrast against lavender background.</done>
</task>

<task type="auto">
  <name>Task 2: Polish calendar visuals and beauty form spacing</name>
  <files>
    src/components/schedule/calendar-grid.tsx,
    src/components/schedule/day-cell.tsx,
    src/components/beauty/product-form.tsx
  </files>
  <read_first>
    src/components/schedule/calendar-grid.tsx,
    src/components/schedule/day-cell.tsx,
    src/components/beauty/product-form.tsx,
    src/app/globals.css
  </read_first>
  <action>
1. **day-cell.tsx** -- Improve calendar day cell visual treatment:
   - Add subtle border: `border border-border/30` on each day cell for grid definition
   - Improve today highlight: use `bg-accent/20 ring-2 ring-accent` or similar accent-colored background + ring to make today clearly stand out
   - Job dots: increase size slightly if tiny (min `w-1.5 h-1.5`), use `bg-accent` color for consistency
   - Income amount display: use `text-xs font-medium text-accent` for amounts to make them readable but not overpowering
   - Day number: `text-sm font-medium` for current month days, `text-muted-foreground` for out-of-month days
   - Empty days: subtle `bg-muted/30` or keep transparent, but ensure consistent cell height with `min-h-[80px]` or similar

2. **calendar-grid.tsx** -- Calendar container improvements:
   - Ensure calendar has `bg-white` or `bg-card` background
   - Add `shadow-sm rounded-xl` to calendar container for card-like treatment
   - Day-of-week header row: use `text-xs font-medium text-muted-foreground uppercase tracking-wider` for weekday labels
   - Add `p-2` or `p-3` internal padding to the calendar container

3. **product-form.tsx** -- Beauty form spacing (same pattern as job-form):
   - Form container padding: `p-6` (24px)
   - Gap between fields: `gap-4` (16px) minimum
   - Gap between labels and inputs: `gap-2` (8px)
   - Gap before submit button: `gap-6` (24px) or `pt-4` spacer
   - Ensure all labels use consistent `text-sm font-medium` styling
  </action>
  <verify>
    <automated>cd D:/Learn/TestGSD && npx vitest run --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - grep "border" src/components/schedule/day-cell.tsx returns matches (cell borders added)
    - grep "ring\|bg-accent" src/components/schedule/day-cell.tsx returns matches (today highlight)
    - grep "shadow" src/components/schedule/calendar-grid.tsx returns matches (calendar container shadow)
    - grep "gap-4\|gap-5\|gap-6" src/components/beauty/product-form.tsx returns matches (form spacing)
  </acceptance_criteria>
  <done>Calendar has polished day cells with borders, clear today highlight, and readable income. Beauty product form has generous spacing. All visual changes use design system tokens consistently.</done>
</task>

</tasks>

<verification>
1. `npx vitest run` -- all existing tests pass
2. Visual inspection: schedule page shows cards with visible contrast
3. Visual inspection: calendar has cell borders and clear today highlight
4. Visual inspection: forms have generous spacing between fields
</verification>

<success_criteria>
- Forms have consistent spacing using design system scale (gap-4, gap-6, p-6)
- Calendar day cells have visible borders and clear today highlight
- Cards have shadow/ring for contrast against lavender background
- All components use design system tokens consistently
</success_criteria>

<output>
After completion, create `.planning/phases/06-refactor-ui-ux-optimization/06-02-SUMMARY.md`
</output>
