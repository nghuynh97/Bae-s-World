---
phase: 05-freelance-schedule-income-tracker
plan: 03
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - src/components/schedule/stats-header.tsx
  - src/components/schedule/income-chart.tsx
  - src/components/layout/top-nav.tsx
  - src/components/layout/bottom-tab-bar.tsx
  - src/lib/supabase/middleware.ts
  - src/app/(private)/schedule/page.tsx
  - src/__tests__/middleware/route-protection.test.ts
  - src/__tests__/schedule/stats.test.ts
autonomous: true
requirements:
  - SCHED-04
  - SCHED-07

must_haves:
  truths:
    - "Stats header shows This Month and This Year income with paid/pending breakdown"
    - "Income chart toggles between monthly and yearly stacked bar views"
    - "Schedule link appears in both top nav and bottom tab bar for authenticated users"
    - "/schedule route is protected by middleware -- unauthenticated users redirected to /login"
  artifacts:
    - path: "src/components/schedule/stats-header.tsx"
      provides: "Income summary cards"
      contains: "formatVND"
    - path: "src/components/schedule/income-chart.tsx"
      provides: "Recharts stacked bar chart"
      contains: "BarChart"
    - path: "src/lib/supabase/middleware.ts"
      provides: "Route protection including /schedule"
      contains: "/schedule"
    - path: "src/components/layout/top-nav.tsx"
      provides: "Schedule nav link"
      contains: "/schedule"
    - path: "src/components/layout/bottom-tab-bar.tsx"
      provides: "Schedule tab"
      contains: "/schedule"
  key_links:
    - from: "src/components/schedule/stats-header.tsx"
      to: "src/lib/schedule/format-vnd.ts"
      via: "VND formatting for stat values"
      pattern: "import.*formatVND.*from.*schedule/format-vnd"
    - from: "src/components/schedule/income-chart.tsx"
      to: "recharts"
      via: "chart library import"
      pattern: "import.*BarChart.*from.*recharts"
    - from: "src/lib/supabase/middleware.ts"
      to: "/schedule"
      via: "route protection check"
      pattern: "pathname\\.startsWith.*schedule"
---

<objective>
Add income statistics with charts, navigation links, and route protection for the schedule feature.

Purpose: Complete the schedule feature by adding the stats header with paid/pending breakdowns, income charts for monthly/yearly trends, navigation access from both top nav and bottom tab bar, and middleware route protection.
Output: Stats cards above calendar, stacked bar charts with period toggle, Schedule in nav, /schedule protected.
</objective>

<execution_context>
@C:/Users/ADMIN/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/ADMIN/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-freelance-schedule-income-tracker/05-CONTEXT.md
@.planning/phases/05-freelance-schedule-income-tracker/05-RESEARCH.md
@.planning/phases/05-freelance-schedule-income-tracker/05-UI-SPEC.md
@.planning/phases/05-freelance-schedule-income-tracker/05-01-SUMMARY.md

<interfaces>
<!-- From Plan 01 outputs -->

From src/actions/schedule.ts:
```typescript
export async function getIncomeStats(year: number, month: number): Promise<{
  totalPaid: number;
  totalPending: number;
  total: number;
  jobCount: number;
}>;
export async function getYearlyStats(year: number): Promise<Array<{
  month: number;
  totalPaid: number;
  totalPending: number;
  total: number;
}>>;
export async function getJobsForMonth(year: number, month: number): Promise<Job[]>;
```

From src/lib/schedule/format-vnd.ts:
```typescript
export function formatVND(amount: number): string;
export function formatVNDCompact(amount: number): string;
```

From src/components/layout/top-nav.tsx:
```typescript
// authLinks array at line 25-31 -- add { href: "/schedule", label: "Schedule" }
```

From src/components/layout/bottom-tab-bar.tsx:
```typescript
// authTabs array at line 34-39 -- add { href: "/schedule", label: "Schedule", icon: CalendarDays }
```

From src/lib/supabase/middleware.ts:
```typescript
// Protected route check at line 38-43 -- add pathname.startsWith("/schedule")
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Stats header, income chart, and stats tests</name>
  <files>src/components/schedule/stats-header.tsx, src/components/schedule/income-chart.tsx, src/__tests__/schedule/stats.test.ts</files>
  <read_first>src/actions/schedule.ts, src/lib/schedule/format-vnd.ts, .planning/phases/05-freelance-schedule-income-tracker/05-UI-SPEC.md, .planning/phases/05-freelance-schedule-income-tracker/05-RESEARCH.md</read_first>
  <action>
1. Install recharts: `npm install recharts`

2. Create src/components/schedule/stats-header.tsx ("use client"):
   - Props: monthStats: { totalPaid, totalPending, total }, yearStats: { totalPaid, totalPending, total }, monthLabel: string (e.g., "March 2026"), yearLabel: string (e.g., "2026")
   - Layout: horizontal scroll on mobile (flex overflow-x-auto snap-x), 2-column grid on tablet+ (sm:grid sm:grid-cols-2)
   - Two cards, each a white card with rounded-lg shadow-sm p-6:
     Card 1 - "This Month":
       - Label: "This Month" in 12px Inter text-secondary
       - Sub-label: monthLabel (e.g., "March 2026") in 12px Inter text-secondary
       - Total amount: 28px Playfair Display bold text-primary, formatVND(monthStats.total)
       - Breakdown line: 12px Inter -- "Paid: {formatVND(paid)}" in text-[var(--color-paid)] + " | Pending: {formatVND(pending)}" in text-[var(--color-pending)]
     Card 2 - "This Year":
       - Same structure with yearStats and yearLabel
   - Gap between cards: gap-4 (16px)
   - Snap: snap-start on each card for mobile scroll

3. Create src/components/schedule/income-chart.tsx:
   - Wrap in next/dynamic with ssr: false since Recharts is client-only:
     ```typescript
     import dynamic from "next/dynamic";
     const IncomeChart = dynamic(() => import("./income-chart-inner"), { ssr: false });
     export { IncomeChart };
     ```
   - Actually create src/components/schedule/income-chart-inner.tsx ("use client") as the real component:
   - Props: monthlyData: Array<{ label: string, paid: number, pending: number }>, yearlyData: Array<{ label: string, paid: number, pending: number }>
   - State: period ("monthly" | "yearly"), default "monthly"
   - Segmented control toggle: two buttons "Monthly" | "Yearly"
     - Active: bg-accent text-white rounded-md px-3 py-1
     - Inactive: text-secondary px-3 py-1
     - Container: bg-muted rounded-lg p-1 flex
   - Chart: Recharts BarChart wrapped in ResponsiveContainer width="100%" height={200}
     - XAxis: dataKey="label", tick fontSize 12
     - YAxis: tickFormatter using formatVNDCompact
     - Tooltip: formatter using formatVND for full display
     - Two stacked Bar components (stackId="income"):
       - Bar dataKey="paid" fill="var(--color-paid)" radius={[4, 4, 0, 0]}
       - Bar dataKey="pending" fill="var(--color-pending)" radius={[4, 4, 0, 0]}
   - Card wrapper: bg-surface rounded-lg shadow-sm p-6
   - Empty state: when all data values are 0, show "No income data for this period." centered text
   - Monthly data: weeks of month (W1, W2, W3, W4, W5 labels)
   - Yearly data: months (Jan, Feb, ..., Dec labels)

4. Create src/__tests__/schedule/stats.test.ts:
   - Test income aggregation logic: given array of jobs, verify totalPaid = sum of paid jobs, totalPending = sum of pending jobs
   - Test yearly grouping: verify jobs grouped correctly by month
   - Test empty month returns zeros
  </action>
  <verify>
    <automated>npx vitest run src/__tests__/schedule/stats.test.ts --reporter=verbose</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "formatVND" src/components/schedule/stats-header.tsx
    - grep -q "This Month" src/components/schedule/stats-header.tsx
    - grep -q "This Year" src/components/schedule/stats-header.tsx
    - grep -q "color-paid" src/components/schedule/stats-header.tsx
    - grep -q "color-pending" src/components/schedule/stats-header.tsx
    - grep -q "BarChart" src/components/schedule/income-chart-inner.tsx
    - grep -q "ResponsiveContainer" src/components/schedule/income-chart-inner.tsx
    - grep -q "stackId" src/components/schedule/income-chart-inner.tsx
    - grep -q "formatVNDCompact" src/components/schedule/income-chart-inner.tsx
    - grep -q "ssr.*false" src/components/schedule/income-chart.tsx
  </acceptance_criteria>
  <done>Stats header shows two cards (This Month / This Year) with paid/pending VND breakdown. Income chart renders stacked bar chart with monthly/yearly toggle. Charts dynamically imported with ssr:false. Stats tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Navigation links, middleware protection, and page wiring</name>
  <files>src/components/layout/top-nav.tsx, src/components/layout/bottom-tab-bar.tsx, src/lib/supabase/middleware.ts, src/app/(private)/schedule/page.tsx, src/__tests__/middleware/route-protection.test.ts</files>
  <read_first>src/components/layout/top-nav.tsx, src/components/layout/bottom-tab-bar.tsx, src/lib/supabase/middleware.ts, src/__tests__/middleware/route-protection.test.ts, src/app/(private)/schedule/page.tsx</read_first>
  <action>
1. Update src/components/layout/top-nav.tsx:
   - Add Schedule link to authLinks array (between "Beauty" and "Journal"):
     { href: "/schedule", label: "Schedule" }
   - Final authLinks order: Portfolio, Beauty, Schedule, Journal, About, Admin

2. Update src/components/layout/bottom-tab-bar.tsx:
   - Import CalendarDays from "lucide-react" (add to existing import)
   - Add Schedule tab to authTabs array (between "Beauty" and "Journal"):
     { href: "/schedule", label: "Schedule", icon: CalendarDays }
   - Final authTabs order: Portfolio, Beauty, Schedule, Journal, Admin, Profile
   - NOTE: This makes 6 tabs. If too crowded, move "Admin" out (but keep for now as existing pattern has 5 tabs and adding 1 more is acceptable on mobile)

3. Update src/lib/supabase/middleware.ts:
   - Add pathname.startsWith("/schedule") to the protected routes condition (line 40 area)
   - Add it alongside existing checks:
     ```
     pathname.startsWith("/dashboard") ||
     pathname.startsWith("/beauty") ||
     pathname.startsWith("/schedule") ||
     pathname.startsWith("/journal") ||
     ...
     ```

4. Update src/app/(private)/schedule/page.tsx to wire in StatsHeader and IncomeChart:
   - Import StatsHeader and IncomeChart components
   - Call getIncomeStats(year, month) for month stats
   - Call getYearlyStats(year) for year stats
   - Compute monthlyChartData: group month jobs by week number, aggregate paid/pending per week (W1-W5)
   - Pass yearlyStats as yearlyChartData with month labels (Jan, Feb, etc.)
   - Render layout top-to-bottom:
     a. StatsHeader with monthStats and yearStats
     b. Gap (48px -- space-y-12 or mb-12)
     c. CalendarHeader
     d. CalendarGrid (with day detail integration from Plan 02)
     e. Gap (48px)
     f. IncomeChart with monthlyData and yearlyData
   - Add --color-pending CSS variable: in the page or a parent layout, add style or ensure the Tailwind config has the pending color. If using Tailwind v4 CSS variables, add to the app globals.css or the schedule page:
     Actually, add to src/app/globals.css under existing theme variables:
     --color-pending: #D97706;
     (Only if not already defined. Check first.)

5. Update src/__tests__/middleware/route-protection.test.ts:
   - Add test case: "redirects unauthenticated users from /schedule to /login"
   - Follow existing test pattern in the file
  </action>
  <verify>
    <automated>npx vitest run src/__tests__/middleware/route-protection.test.ts --reporter=verbose</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "/schedule" src/components/layout/top-nav.tsx
    - grep -q "Schedule" src/components/layout/top-nav.tsx
    - grep -q "/schedule" src/components/layout/bottom-tab-bar.tsx
    - grep -q "CalendarDays" src/components/layout/bottom-tab-bar.tsx
    - grep -q "/schedule" src/lib/supabase/middleware.ts
    - grep -q "StatsHeader" src/app/\(private\)/schedule/page.tsx
    - grep -q "IncomeChart" src/app/\(private\)/schedule/page.tsx
    - grep -q "getIncomeStats" src/app/\(private\)/schedule/page.tsx
    - grep -q "getYearlyStats" src/app/\(private\)/schedule/page.tsx
    - grep -q "schedule" src/__tests__/middleware/route-protection.test.ts
  </acceptance_criteria>
  <done>Schedule link in top nav and bottom tab bar for authenticated users. /schedule protected by middleware (unauthenticated redirected to /login). Schedule page wired with stats header and income chart. Route protection test updated and passing.</done>
</task>

</tasks>

<verification>
- `npx vitest run src/__tests__/schedule/ src/__tests__/middleware/route-protection.test.ts --reporter=verbose` -- all tests pass
- `npx tsc --noEmit` -- no TypeScript errors
- /schedule appears in middleware protected routes
- Schedule link visible in nav for authenticated users
- Stats header renders above calendar with income breakdown
- Chart toggles between monthly and yearly views
</verification>

<success_criteria>
- Stats header shows This Month and This Year cards with total, paid, and pending VND amounts
- Income chart renders stacked bar chart (paid green, pending orange) with monthly/yearly toggle
- Charts dynamically imported (no SSR) to avoid bundle bloat
- Schedule link appears in both desktop top-nav and mobile bottom-tab-bar
- /schedule route redirected to /login for unauthenticated users
- Schedule page integrates stats header, calendar, and chart in correct layout order
</success_criteria>

<output>
After completion, create `.planning/phases/05-freelance-schedule-income-tracker/05-03-SUMMARY.md`
</output>
