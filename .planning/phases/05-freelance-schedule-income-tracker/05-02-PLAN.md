---
phase: 05-freelance-schedule-income-tracker
plan: 02
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - src/app/(private)/schedule/page.tsx
  - src/app/(private)/schedule/loading.tsx
  - src/components/schedule/calendar-header.tsx
  - src/components/schedule/calendar-grid.tsx
  - src/components/schedule/day-cell.tsx
  - src/components/schedule/day-detail.tsx
  - src/components/schedule/job-card.tsx
  - src/components/schedule/job-form.tsx
  - src/components/schedule/schedule-empty.tsx
autonomous: true
requirements:
  - SCHED-02
  - SCHED-06
  - SCHED-01

must_haves:
  truths:
    - "User sees a month grid calendar with 7 columns starting Monday"
    - "Day cells show job count dots (green=paid, orange=pending) and compact VND income"
    - "Tapping a day with jobs shows job list below the calendar"
    - "Tapping an empty day opens the add job form sheet"
    - "User can add a job with all required fields via the slide-up sheet form"
    - "User can edit an existing job by tapping its card"
    - "User can delete a job from the edit form"
    - "Today's date has an accent ring highlight"
  artifacts:
    - path: "src/app/(private)/schedule/page.tsx"
      provides: "Server component schedule page"
      contains: "getJobsForMonth"
    - path: "src/components/schedule/calendar-grid.tsx"
      provides: "Month grid with day cells"
      contains: "grid-cols-7"
    - path: "src/components/schedule/day-cell.tsx"
      provides: "Day cell with dots and income"
      contains: "formatVNDCompact"
    - path: "src/components/schedule/job-form.tsx"
      provides: "Add/edit job form in Sheet"
      contains: "createJob"
    - path: "src/components/schedule/job-card.tsx"
      provides: "Job card with status stripe"
      contains: "color-paid"
  key_links:
    - from: "src/app/(private)/schedule/page.tsx"
      to: "src/actions/schedule.ts"
      via: "server action import for data fetching"
      pattern: "import.*getJobsForMonth.*from.*actions/schedule"
    - from: "src/components/schedule/job-form.tsx"
      to: "src/actions/schedule.ts"
      via: "createJob/updateJob/deleteJob calls"
      pattern: "import.*createJob.*from.*actions/schedule"
    - from: "src/components/schedule/day-cell.tsx"
      to: "src/lib/schedule/format-vnd.ts"
      via: "compact VND formatting"
      pattern: "import.*formatVNDCompact.*from.*schedule/format-vnd"
    - from: "src/components/schedule/calendar-grid.tsx"
      to: "src/lib/schedule/date-utils.ts"
      via: "calendar day generation"
      pattern: "import.*getCalendarDays.*from.*schedule/date-utils"
---

<objective>
Build the calendar month view, day detail panel, job cards, and job form sheet for the schedule tracker.

Purpose: Create the core interactive calendar UI where Funnghy can view her schedule by month, see jobs on each day, and add/edit/delete jobs. This is the primary interaction surface of the schedule feature.
Output: Full calendar page with day cells showing job dots + income, day detail panel with job cards, and slide-up sheet form for job CRUD.
</objective>

<execution_context>
@C:/Users/ADMIN/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/ADMIN/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-freelance-schedule-income-tracker/05-CONTEXT.md
@.planning/phases/05-freelance-schedule-income-tracker/05-RESEARCH.md
@.planning/phases/05-freelance-schedule-income-tracker/05-UI-SPEC.md
@.planning/phases/05-freelance-schedule-income-tracker/05-01-SUMMARY.md

<interfaces>
<!-- From Plan 01 outputs (executor reads the actual files, these are the contracts) -->

From src/lib/db/schema.ts:
```typescript
export const scheduleJobs = pgTable("schedule_jobs", {
  id: uuid("id").defaultRandom().primaryKey(),
  jobDate: text("job_date").notNull(),
  clientName: text("client_name").notNull(),
  location: text("location").notNull(),
  startTime: text("start_time").notNull(),
  endTime: text("end_time").notNull(),
  payAmount: integer("pay_amount").notNull(),
  status: text("status").notNull().default("pending"),
  notes: text("notes"),
  createdAt: timestamp("created_at").defaultNow().notNull(),
  updatedAt: timestamp("updated_at").defaultNow().notNull(),
});
```

From src/actions/schedule.ts:
```typescript
export async function createJob(data: { jobDate, clientName, location, startTime, endTime, payAmount, status, notes? }): Promise<Job>;
export async function getJobsForMonth(year: number, month: number): Promise<Job[]>;
export async function getJobById(jobId: string): Promise<Job | null>;
export async function updateJob(jobId: string, data: Partial<JobFields>): Promise<Job>;
export async function deleteJob(jobId: string): Promise<{ success: true }>;
```

From src/lib/schedule/format-vnd.ts:
```typescript
export function formatVND(amount: number): string;
export function formatVNDCompact(amount: number): string;
```

From src/lib/schedule/date-utils.ts:
```typescript
export function getCalendarDays(currentMonth: Date): Date[];
```

From src/components/ui/sheet.tsx:
```typescript
// Sheet, SheetTrigger, SheetPortal, SheetClose, SheetOverlay, SheetContent, SheetHeader, SheetTitle
// Built on @base-ui/react/dialog primitives
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Calendar grid, header, day cells, and schedule page</name>
  <files>src/app/(private)/schedule/page.tsx, src/app/(private)/schedule/loading.tsx, src/components/schedule/calendar-header.tsx, src/components/schedule/calendar-grid.tsx, src/components/schedule/day-cell.tsx, src/components/schedule/schedule-empty.tsx</files>
  <read_first>src/lib/schedule/date-utils.ts, src/lib/schedule/format-vnd.ts, src/actions/schedule.ts, src/lib/db/schema.ts, src/components/ui/sheet.tsx, src/components/beauty/product-form.tsx, .planning/phases/05-freelance-schedule-income-tracker/05-UI-SPEC.md</read_first>
  <action>
1. Create src/app/(private)/schedule/page.tsx as a SERVER COMPONENT:
   - Accept searchParams: Promise<{ month?: string; year?: string }>
   - Parse month/year from searchParams (default to current date)
   - Call getJobsForMonth(year, month) server action
   - Render layout: StatsHeader placeholder div (Plan 03 will fill), CalendarHeader, CalendarGrid
   - Pass jobs array and year/month to client components as props

2. Create src/app/(private)/schedule/loading.tsx:
   - Import Skeleton from "@/components/ui/skeleton"
   - 2 skeleton stat cards (h-20 w-full), skeleton calendar header (h-8 w-48 centered), 35 skeleton day cells in 7x5 grid
   - Use animate-pulse on all skeletons

3. Create src/components/schedule/calendar-header.tsx ("use client"):
   - Props: year: number, month: number
   - Display month/year formatted with date-fns format(new Date(year, month - 1), "MMMM yyyy")
   - Left chevron (ChevronLeft from lucide) and right chevron (ChevronRight) as navigation buttons
   - Navigation uses router.push with updated search params: ?month=X&year=Y
   - When month goes below 1, decrement year and set month to 12. When above 12, increment year and set month to 1
   - "Today" text button shown when not viewing current month -- navigates to current month/year
   - Heading: 20px Playfair Display bold, centered. Arrows: 44px touch target buttons
   - Font classes: font-display text-xl font-bold for title

4. Create src/components/schedule/calendar-grid.tsx ("use client"):
   - Props: jobs: Job[], year: number, month: number
   - State: selectedDate (Date | null)
   - Call getCalendarDays(new Date(year, month - 1)) to get day array
   - Group jobs by jobDate string for O(1) lookup: Record<string, Job[]>
   - Render weekday headers row: "Mon", "Tue", "Wed", "Thu", "Fri", "Sat", "Sun" -- 12px Inter, text-secondary, centered
   - Render CSS grid: grid grid-cols-7 gap-[1px] bg-border (grid-line effect)
   - Each cell renders DayCell component
   - Pass selectedDate and onSelectDate callback to each DayCell
   - Below the grid, conditionally render DayDetail panel when selectedDate has jobs
   - When selectedDate is set but has no jobs, open job form sheet for that date

5. Create src/components/schedule/day-cell.tsx ("use client"):
   - Props: date: Date, jobs: Job[], isCurrentMonth: boolean, isToday: boolean, isSelected: boolean, onSelect: (date: Date) => void
   - Layout: vertical stack inside cell. Min height 44px on mobile (min-h-[44px])
   - Day number top-left: 12px Inter, text-primary if current month, text-secondary/40 if not
   - Today: ring-2 ring-accent, font-bold on day number
   - Selected: bg-accent/10 border border-accent
   - Non-current-month days: show number but muted, not interactive (onClick does nothing)
   - Job indicator dots: max 3 visible, 6px circles arranged horizontally with 4px gap
     - Paid job dot: bg-[var(--color-paid)] (which is --color-success / #059669)
     - Pending job dot: bg-[var(--color-pending)] (amber-600 / #D97706)
   - Income total at bottom: 10px Inter, text-secondary, formatVNDCompact(sum of payAmounts)
   - Only show dots and income if jobs.length > 0

6. Create src/components/schedule/schedule-empty.tsx:
   - CalendarDays icon from lucide, 48px, text-accent, centered
   - Heading: "No jobs yet", 20px Playfair Display bold, text-primary
   - Body: "Tap a date to add your first job.", 14px Inter, text-secondary
   - Centered vertically in content area, 16px gap between elements
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "getJobsForMonth" src/app/\(private\)/schedule/page.tsx
    - grep -q "searchParams" src/app/\(private\)/schedule/page.tsx
    - grep -q "grid-cols-7" src/components/schedule/calendar-grid.tsx
    - grep -q "getCalendarDays" src/components/schedule/calendar-grid.tsx
    - grep -q "formatVNDCompact" src/components/schedule/day-cell.tsx
    - grep -q "color-paid" src/components/schedule/day-cell.tsx
    - grep -q "color-pending" src/components/schedule/day-cell.tsx
    - grep -q "ring-accent" src/components/schedule/day-cell.tsx
    - grep -q "ChevronLeft" src/components/schedule/calendar-header.tsx
    - grep -q "CalendarDays" src/components/schedule/schedule-empty.tsx
    - grep -q "Skeleton" src/app/\(private\)/schedule/loading.tsx
  </acceptance_criteria>
  <done>Calendar month grid renders with 7 columns starting Monday, day cells show job dots (green/orange by status) and compact VND income, today has accent ring, month navigation updates URL params, loading skeleton shows on initial load</done>
</task>

<task type="auto">
  <name>Task 2: Day detail panel, job cards, and job form sheet</name>
  <files>src/components/schedule/day-detail.tsx, src/components/schedule/job-card.tsx, src/components/schedule/job-form.tsx</files>
  <read_first>src/components/ui/sheet.tsx, src/components/beauty/product-form.tsx, src/actions/schedule.ts, src/components/schedule/calendar-grid.tsx, .planning/phases/05-freelance-schedule-income-tracker/05-UI-SPEC.md</read_first>
  <action>
1. Create src/components/schedule/day-detail.tsx ("use client"):
   - Props: date: Date, jobs: Job[], onAddJob: () => void, onEditJob: (jobId: string) => void
   - Header: formatted as "Thursday, March 20, 2026" using date-fns format(date, "EEEE, MMMM d, yyyy"), 14px Inter bold
   - Job count badge: muted pill showing "N jobs" or "1 job" (singular/plural) -- bg-muted rounded-full px-2 py-0.5 text-xs
   - Content: vertical list of JobCard components for each job
   - Empty state for selected day with no jobs: "No jobs on this day. Tap + to add one." -- text-secondary, centered
   - "Add Job" button at bottom: accent background, full width, text "Add Job"
   - Animation: add transition class for slide-down effect (animate-in slide-in-from-top-2 duration-200)
   - Spacing: 8px gap between job cards, 16px padding

2. Create src/components/schedule/job-card.tsx ("use client"):
   - Props: job: Job, onEdit: (jobId: string) => void
   - Layout: horizontal card with left color stripe (4px wide vertical bar)
   - Left stripe color: var(--color-paid) if job.status === "paid", var(--color-pending) if "pending"
   - Content left-to-right:
     - Time range: 14px Inter regular, text-secondary, "{startTime} - {endTime}", fixed w-20
     - Main info vertical stack: client name (14px Inter bold, text-primary, truncate), location (12px Inter, text-secondary, truncate, with MapPin icon 12px inline)
     - Pay amount: 14px Inter bold, right-aligned, text-primary, formatVND(job.payAmount)
   - Status badge below pay: pill style, 10px Inter
     - Paid: bg-[var(--color-paid)]/15 text-[var(--color-paid)] content "Paid"
     - Pending: bg-[var(--color-pending)]/15 text-[var(--color-pending)] content "Pending"
   - Card style: bg-surface rounded-[10px] shadow-sm p-4
   - onClick: calls onEdit(job.id)

3. Create src/components/schedule/job-form.tsx ("use client"):
   - Props: date: Date, job?: Job (for edit mode), open: boolean, onOpenChange: (open: boolean) => void
   - Uses Sheet component with side="bottom" (or configure SheetContent for bottom slide-up)
   - Max height: max-h-[90vh], rounded-t-2xl
   - Drag indicator: 40px wide, 4px tall, centered, bg-muted-foreground/30, rounded-full, mt-2
   - Title: "Add Job" or "Edit Job" (based on job prop), 20px Playfair Display bold
   - Date display: format(date, "MMMM d, yyyy"), read-only, 14px Inter, text-secondary
   - Form fields using react-hook-form + zod (follow product-form.tsx pattern):
     a. Client Name: Input, required, placeholder "Client name"
     b. Location: Input, required, placeholder "Location"
     c. Start Time + End Time: side by side in flex row, type="time" inputs, defaults "09:00" and "17:00", "to" label between
     d. Pay Amount: Input type="number", required, placeholder "0", with "VND" text suffix. On blur format with dots for display, on focus show raw integer. Use a separate display state vs form value.
     e. Status: Two-button segmented control "Pending" | "Paid"
        - Pending active: bg-[var(--color-pending)]/15 text-[var(--color-pending)] border border-[var(--color-pending)]
        - Paid active: bg-[var(--color-paid)]/15 text-[var(--color-paid)] border border-[var(--color-paid)]
        - Default to "pending" for new jobs
     f. Notes: Textarea, optional, placeholder "Notes about this job...", 3 rows
   - Submit button: "Add Job" or "Save Changes", accent bg, full width
   - Delete button (edit mode only): "Delete Job", destructive outline variant, full width, 16px below submit
   - Delete flow: show Dialog confirmation "Delete this job?" / "This job for [client name] on [date] will be permanently removed." / Cancel + Delete buttons
   - On submit: call createJob or updateJob server action, show toast ("Job added" / "Job updated"), call router.refresh(), close sheet
   - On delete: call deleteJob, show toast "Job deleted", router.refresh(), close sheet
   - Key prop: use key={date.toISOString()} on the form to force reset when date changes
   - Field spacing: space-y-4 (16px between fields)
   - Validation: inline error messages in text-destructive below each field
   - Import toast from sonner
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "EEEE, MMMM d, yyyy" src/components/schedule/day-detail.tsx
    - grep -q "onAddJob" src/components/schedule/day-detail.tsx
    - grep -q "color-paid" src/components/schedule/job-card.tsx
    - grep -q "color-pending" src/components/schedule/job-card.tsx
    - grep -q "MapPin" src/components/schedule/job-card.tsx
    - grep -q "formatVND" src/components/schedule/job-card.tsx
    - grep -q "createJob" src/components/schedule/job-form.tsx
    - grep -q "updateJob" src/components/schedule/job-form.tsx
    - grep -q "deleteJob" src/components/schedule/job-form.tsx
    - grep -q "useForm" src/components/schedule/job-form.tsx
    - grep -q "Sheet" src/components/schedule/job-form.tsx
    - grep -q "router.refresh" src/components/schedule/job-form.tsx
    - grep -q "toast" src/components/schedule/job-form.tsx
  </acceptance_criteria>
  <done>Day detail panel shows below calendar with job cards (left stripe color by status), job form sheet slides up with all fields (client, location, times, pay, status, notes), form handles create/edit/delete with server actions and toast feedback, form resets on date change</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` -- no TypeScript errors across all schedule components
- Schedule page renders at /schedule with calendar grid
- Day cells show job dots and income amounts
- Tapping a day with jobs shows day detail below calendar
- Job form sheet opens for add/edit with all fields
- Delete confirmation dialog works
</verification>

<success_criteria>
- Calendar month grid renders 7 columns starting Monday with correct days for any month
- Day cells display paid (green) and pending (orange) job dots, compact VND income totals
- Today has accent ring highlight, selected day has accent background/border
- Day detail panel appears below calendar showing job cards with status stripe, time, client, location, pay
- Job form sheet slides up from bottom with all fields, zod validation, create/edit/delete flows
- Month navigation via chevron arrows updates URL params and refetches data
- Loading skeleton shows while page loads
</success_criteria>

<output>
After completion, create `.planning/phases/05-freelance-schedule-income-tracker/05-02-SUMMARY.md`
</output>
