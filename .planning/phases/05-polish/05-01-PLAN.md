---
phase: 05-polish
plan: 01
type: execute
wave: 1
depends_on: ['05-00']
files_modified:
  - src/app/globals.css
  - src/app/(public)/template.tsx
  - src/app/(private)/template.tsx
  - src/app/(auth)/template.tsx
  - src/components/ui/button-spinner.tsx
  - src/components/ui/sonner.tsx
  - src/app/(private)/dashboard/page.tsx
  - src/components/schedule/job-card.tsx
  - src/components/schedule/stats-header.tsx
  - src/components/beauty/product-grid.tsx
autonomous: true
requirements:
  - POLISH-01
  - POLISH-02
  - POLISH-04
must_haves:
  truths:
    - 'Every route change shows a subtle fade-in animation'
    - 'Cards show a barely-visible lift on hover'
    - 'Buttons and tappable elements scale to 0.97x on active press'
    - 'Toasts appear from top-center and auto-dismiss after 3 seconds'
    - 'A reusable ButtonSpinner component exists for form loading states'
  artifacts:
    - path: 'src/app/(public)/template.tsx'
      provides: 'Fade-in wrapper for public routes'
      contains: 'motion-safe:animate-page-fade-in'
    - path: 'src/app/(private)/template.tsx'
      provides: 'Fade-in wrapper for private routes'
      contains: 'motion-safe:animate-page-fade-in'
    - path: 'src/app/(auth)/template.tsx'
      provides: 'Fade-in wrapper for auth routes'
      contains: 'motion-safe:animate-page-fade-in'
    - path: 'src/components/ui/button-spinner.tsx'
      provides: 'Reusable spinner component for buttons'
      exports: ['ButtonSpinner']
    - path: 'src/app/globals.css'
      provides: 'Page fade-in keyframes'
      contains: '@keyframes page-fade-in'
  key_links:
    - from: 'src/app/(public)/template.tsx'
      to: 'src/app/globals.css'
      via: 'animate-page-fade-in class using @keyframes'
      pattern: 'animate-page-fade-in'
    - from: 'src/components/ui/sonner.tsx'
      to: 'Toaster in root layout'
      via: 'position and duration props'
      pattern: 'position.*top-center'
---

<objective>
Add CSS animation foundations, page fade transitions, hover/press effects on cards, toast configuration, and the ButtonSpinner component.

Purpose: Establish all polish infrastructure and apply hover/press/fade effects globally so the app feels smooth and responsive.
Output: template.tsx files for all route groups, updated globals.css with keyframes, ButtonSpinner component, updated sonner config, hover effects on dashboard and schedule cards.
</objective>

<execution_context>
@C:/Users/ADMIN/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/ADMIN/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-polish/05-CONTEXT.md
@.planning/phases/05-polish/05-RESEARCH.md

@src/app/globals.css
@src/components/ui/sonner.tsx
@src/app/(private)/dashboard/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: CSS keyframes, template.tsx fade-ins, ButtonSpinner, and toast config</name>
  <files>
    src/app/globals.css,
    src/app/(public)/template.tsx,
    src/app/(private)/template.tsx,
    src/app/(auth)/template.tsx,
    src/components/ui/button-spinner.tsx,
    src/components/ui/sonner.tsx
  </files>
  <read_first>
    src/app/globals.css,
    src/components/ui/sonner.tsx,
    src/app/(public)/layout.tsx,
    src/app/(private)/layout.tsx,
    src/app/(auth)/login/page.tsx
  </read_first>
  <action>
1. In `src/app/globals.css`, add AFTER the existing `@layer base` block:

```css
@layer utilities {
  .animate-page-fade-in {
    animation: page-fade-in 250ms ease-out;
  }
}

@keyframes page-fade-in {
  from {
    opacity: 0;
  }
  to {
    opacity: 1;
  }
}
```

2. Create three identical `template.tsx` files for each route group -- `(public)`, `(private)`, `(auth)`:

```tsx
export default function Template({ children }: { children: React.ReactNode }) {
  return <div className="motion-safe:animate-page-fade-in">{children}</div>;
}
```

3. Create `src/components/ui/button-spinner.tsx`:

```tsx
import { Loader2 } from 'lucide-react';
import { cn } from '@/lib/utils';

export function ButtonSpinner({ className }: { className?: string }) {
  return (
    <Loader2
      className={cn('size-4 animate-spin', className)}
      aria-hidden="true"
    />
  );
}
```

4. Update `src/components/ui/sonner.tsx` -- add `position="top-center"` and `duration={3000}` props to the Sonner component. Keep all existing props (theme, className, icons, style, toastOptions). Add the two new props before the spread `{...props}`.
   </action>
   <verify>
   <automated>npx vitest run src/**tests**/polish/page-fade.test.tsx src/**tests**/polish/button-spinner.test.tsx src/**tests**/polish/toast-config.test.tsx --reporter=verbose 2>&1 | tail -20; echo "---"; grep -l "animate-page-fade-in" src/app/globals.css src/app/\(public\)/template.tsx src/app/\(private\)/template.tsx src/app/\(auth\)/template.tsx 2>/dev/null | wc -l</automated>
   </verify>
   <acceptance_criteria> - grep "page-fade-in" src/app/globals.css returns the @keyframes block - grep "motion-safe:animate-page-fade-in" on all three template.tsx files succeeds - grep "ButtonSpinner" src/components/ui/button-spinner.tsx returns the export - grep "position.*top-center" src/components/ui/sonner.tsx succeeds - grep "duration.*3000" src/components/ui/sonner.tsx succeeds - npx vitest run src/**tests**/polish/page-fade.test.tsx passes - npx vitest run src/**tests**/polish/button-spinner.test.tsx passes - npx vitest run src/**tests**/polish/toast-config.test.tsx passes
   </acceptance_criteria>
   <done>Page fade-in animation plays on every route change via template.tsx. ButtonSpinner component is ready for use. Toasts appear from top-center with 3s auto-dismiss.</done>
   </task>

<task type="auto">
  <name>Task 2: Hover lift and active press effects on cards and buttons</name>
  <files>
    src/app/(private)/dashboard/page.tsx,
    src/components/schedule/job-card.tsx,
    src/components/schedule/stats-header.tsx,
    src/components/beauty/product-grid.tsx
  </files>
  <read_first>
    src/app/(private)/dashboard/page.tsx,
    src/components/schedule/job-card.tsx,
    src/components/schedule/stats-header.tsx,
    src/components/beauty/product-grid.tsx,
    src/components/portfolio/gallery-card.tsx
  </read_first>
  <action>
Apply subtle hover lift and active press effects to interactive card elements. The existing portfolio gallery card already has `hover:scale-[1.02]` -- do NOT modify it (locked decision: stays as-is).

Pattern for cards (hover lift):

```
motion-safe:transition-all motion-safe:duration-200 motion-safe:hover:shadow-md motion-safe:hover:-translate-y-0.5
```

Pattern for buttons/tappable elements (active press):

```
transition-transform duration-100 active:scale-[0.97]
```

1. `src/app/(private)/dashboard/page.tsx` -- Update the Card className inside the `.map()`:
   - REPLACE: `className="rounded-[16px] shadow-md bg-surface hover:shadow-lg transition-shadow"`
   - WITH: `className="rounded-[16px] shadow-sm bg-surface motion-safe:transition-all motion-safe:duration-200 motion-safe:hover:shadow-md motion-safe:hover:-translate-y-0.5"`
   - Note: changed shadow-md to shadow-sm as base, hover goes to shadow-md (subtle, not dramatic).

2. `src/components/schedule/job-card.tsx` -- Find the main card/container element and add hover lift classes: `motion-safe:transition-all motion-safe:duration-200 motion-safe:hover:shadow-md motion-safe:hover:-translate-y-0.5`. Also add `active:scale-[0.97]` if the card is clickable/tappable.

3. `src/components/schedule/stats-header.tsx` -- Find the stat card elements and add hover lift: `motion-safe:transition-all motion-safe:duration-200 motion-safe:hover:shadow-md motion-safe:hover:-translate-y-0.5`.

4. `src/components/beauty/product-grid.tsx` -- Find the product card/grid-item elements and add hover lift: `motion-safe:transition-all motion-safe:duration-200 motion-safe:hover:shadow-md motion-safe:hover:-translate-y-0.5`. If items are tappable, also add `active:scale-[0.97]`.

IMPORTANT: Do NOT touch `src/components/portfolio/gallery-card.tsx` -- its existing hover effect stays as-is per locked decision.
</action>
<verify>
<automated>grep -c "motion-safe:hover:shadow-md" src/app/\(private\)/dashboard/page.tsx src/components/schedule/job-card.tsx src/components/schedule/stats-header.tsx src/components/beauty/product-grid.tsx 2>/dev/null; echo "---"; grep -c "hover:scale-\[1.02\]" src/components/portfolio/gallery-card.tsx 2>/dev/null; echo "---"; npx vitest run --reporter=verbose 2>&1 | tail -5</automated>
</verify>
<acceptance_criteria> - grep "motion-safe:hover:shadow-md" returns matches in all 4 modified files - grep "motion-safe:hover:-translate-y-0.5" returns matches in all 4 modified files - grep "hover:scale-[1.02]" in gallery-card.tsx still returns a match (unchanged) - No gallery-card.tsx in the git diff (untouched) - Existing tests still pass: npx vitest run --reporter=verbose
</acceptance_criteria>
<done>Dashboard cards, schedule job cards, stats cards, and beauty product cards all show subtle hover lift. Portfolio gallery card hover unchanged. Active press scale applied to tappable elements.</done>
</task>

</tasks>

<verification>
- `npx vitest run --reporter=verbose` -- all existing tests pass
- `npx vitest run src/__tests__/polish/page-fade.test.tsx src/__tests__/polish/button-spinner.test.tsx src/__tests__/polish/toast-config.test.tsx` -- all pass
- All three template.tsx files contain `motion-safe:animate-page-fade-in`
- globals.css contains `@keyframes page-fade-in`
- sonner.tsx has `position="top-center"` and `duration={3000}`
- ButtonSpinner component exists and exports correctly
- Dashboard, schedule, and beauty cards have hover lift classes
- Portfolio gallery card is NOT modified
</verification>

<success_criteria>

- Page fade-in animation plays on every client-side navigation
- Cards across all sections show subtle shadow + lift on hover
- Toasts appear from top-center with 3s auto-dismiss
- ButtonSpinner component is ready for plan 02
- All animations use motion-safe prefix for accessibility
  </success_criteria>

<output>
After completion, create `.planning/phases/05-polish/05-01-SUMMARY.md`
</output>
