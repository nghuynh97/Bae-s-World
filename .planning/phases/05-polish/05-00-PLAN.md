---
phase: 05-polish
plan: 00
type: execute
wave: 0
depends_on: []
files_modified:
  - src/__tests__/polish/page-fade.test.tsx
  - src/__tests__/polish/button-spinner.test.tsx
  - src/__tests__/polish/loading-skeletons.test.tsx
  - src/__tests__/polish/toast-config.test.tsx
autonomous: true
requirements:
  - POLISH-01
  - POLISH-02
  - POLISH-03
  - POLISH-04
  - POLISH-05
must_haves:
  truths:
    - "Test stubs exist for all polish requirements before implementation begins"
    - "Each test file fails with meaningful assertion (RED phase)"
  artifacts:
    - path: "src/__tests__/polish/page-fade.test.tsx"
      provides: "Test for DESG-04a: page fade animation via template.tsx"
    - path: "src/__tests__/polish/button-spinner.test.tsx"
      provides: "Test for DESG-04b: ButtonSpinner component renders"
    - path: "src/__tests__/polish/loading-skeletons.test.tsx"
      provides: "Test for DESG-04c: loading skeletons exist for main pages"
    - path: "src/__tests__/polish/toast-config.test.tsx"
      provides: "Test for DESG-04e: toast position and duration config"
  key_links: []
---

<objective>
Create test stub files for all polish requirements so that Wave 1 and Wave 2 plans have automated verification targets.

Purpose: Satisfy Nyquist sampling requirement -- every implementation task must have an automated test to run against.
Output: 4 test stub files in src/__tests__/polish/ that fail until their corresponding features are implemented.
</objective>

<execution_context>
@C:/Users/ADMIN/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/ADMIN/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-polish/05-CONTEXT.md
@.planning/phases/05-polish/05-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create 4 polish test stub files</name>
  <files>
    src/__tests__/polish/page-fade.test.tsx,
    src/__tests__/polish/button-spinner.test.tsx,
    src/__tests__/polish/loading-skeletons.test.tsx,
    src/__tests__/polish/toast-config.test.tsx
  </files>
  <read_first>
    src/__tests__/setup.ts,
    src/__tests__/portfolio/masonry-grid.test.tsx,
    vitest.config.ts
  </read_first>
  <action>
Create the `src/__tests__/polish/` directory and 4 test stub files. Each test should import the component/file it will verify and make meaningful assertions. Tests will FAIL initially (RED phase) because the components do not yet exist.

1. **src/__tests__/polish/page-fade.test.tsx** -- Tests for DESG-04a (page fade animation):
```tsx
import { describe, it, expect } from "vitest";
import { render, screen } from "@testing-library/react";

describe("Page fade animation", () => {
  it("public template renders children with fade-in class", async () => {
    const { default: Template } = await import("@/app/(public)/template");
    const { container } = render(<Template><p>test</p></Template>);
    expect(container.firstChild).toHaveClass("motion-safe:animate-page-fade-in");
    expect(screen.getByText("test")).toBeInTheDocument();
  });

  it("private template renders children with fade-in class", async () => {
    const { default: Template } = await import("@/app/(private)/template");
    const { container } = render(<Template><p>test</p></Template>);
    expect(container.firstChild).toHaveClass("motion-safe:animate-page-fade-in");
  });

  it("auth template renders children with fade-in class", async () => {
    const { default: Template } = await import("@/app/(auth)/template");
    const { container } = render(<Template><p>test</p></Template>);
    expect(container.firstChild).toHaveClass("motion-safe:animate-page-fade-in");
  });
});
```

2. **src/__tests__/polish/button-spinner.test.tsx** -- Tests for DESG-04b (ButtonSpinner component):
```tsx
import { describe, it, expect } from "vitest";
import { render } from "@testing-library/react";

describe("ButtonSpinner", () => {
  it("renders a spinning loader icon", async () => {
    const { ButtonSpinner } = await import("@/components/ui/button-spinner");
    const { container } = render(<ButtonSpinner />);
    const svg = container.querySelector("svg");
    expect(svg).toBeTruthy();
    expect(svg?.classList.toString()).toContain("animate-spin");
  });

  it("accepts additional className", async () => {
    const { ButtonSpinner } = await import("@/components/ui/button-spinner");
    const { container } = render(<ButtonSpinner className="text-red-500" />);
    const svg = container.querySelector("svg");
    expect(svg?.classList.toString()).toContain("text-red-500");
  });

  it("has aria-hidden for accessibility", async () => {
    const { ButtonSpinner } = await import("@/components/ui/button-spinner");
    const { container } = render(<ButtonSpinner />);
    const svg = container.querySelector("svg");
    expect(svg?.getAttribute("aria-hidden")).toBe("true");
  });
});
```

3. **src/__tests__/polish/loading-skeletons.test.tsx** -- Tests for DESG-04c (loading skeletons):
```tsx
import { describe, it, expect } from "vitest";
import { render } from "@testing-library/react";

describe("Loading skeletons", () => {
  it("public portfolio loading skeleton renders Skeleton components", async () => {
    const { default: Loading } = await import("@/app/(public)/loading");
    const { container } = render(<Loading />);
    // Skeleton components render as divs with animate-pulse
    const skeletons = container.querySelectorAll("[data-slot='skeleton'], .animate-pulse");
    expect(skeletons.length).toBeGreaterThan(0);
  });

  it("dashboard loading skeleton renders Skeleton components", async () => {
    const { default: Loading } = await import("@/app/(private)/dashboard/loading");
    const { container } = render(<Loading />);
    const skeletons = container.querySelectorAll("[data-slot='skeleton'], .animate-pulse");
    expect(skeletons.length).toBeGreaterThan(0);
  });

  it("beauty loading skeleton renders Skeleton components", async () => {
    const { default: Loading } = await import("@/app/(private)/beauty/loading");
    const { container } = render(<Loading />);
    const skeletons = container.querySelectorAll("[data-slot='skeleton'], .animate-pulse");
    expect(skeletons.length).toBeGreaterThan(0);
  });

  it("schedule loading skeleton exists", async () => {
    const { default: Loading } = await import("@/app/(private)/schedule/loading");
    const { container } = render(<Loading />);
    const skeletons = container.querySelectorAll("[data-slot='skeleton'], .animate-pulse");
    expect(skeletons.length).toBeGreaterThan(0);
  });
});
```

4. **src/__tests__/polish/toast-config.test.tsx** -- Tests for DESG-04e (toast configuration):
```tsx
import { describe, it, expect } from "vitest";
import { render } from "@testing-library/react";

describe("Toast configuration", () => {
  it("Toaster renders with top-center position", async () => {
    const { Toaster } = await import("@/components/ui/sonner");
    const { container } = render(<Toaster />);
    // Sonner renders a section with data-sonner-toaster and data attributes for position
    const toaster = container.querySelector("[data-sonner-toaster]");
    expect(toaster).toBeTruthy();
    expect(toaster?.getAttribute("data-position")).toBe("top-center");
  });
});
```

IMPORTANT: Use dynamic imports (`await import()`) so tests fail at the assertion level when files do not exist yet, rather than at the module resolution level. If vitest cannot resolve the module, wrap each test body in a try-catch that calls `expect.fail("Module not found -- implement feature first")`.

Adapt the test patterns based on what you see in the existing test files (setup.ts, masonry-grid.test.tsx) for consistent style, matchers, and import patterns.
  </action>
  <verify>
    <automated>ls src/__tests__/polish/*.test.tsx 2>/dev/null | wc -l</automated>
  </verify>
  <done>4 test stub files exist in src/__tests__/polish/. They will fail until their corresponding features are implemented in plans 05-01 and 05-02.</done>
</task>

</tasks>

<verification>
- `ls src/__tests__/polish/*.test.tsx` shows 4 files
- Each file contains `describe` and `it` blocks with meaningful assertions
- Tests target the correct import paths matching what plans 05-01 and 05-02 will create
</verification>

<success_criteria>
- 4 test stub files created: page-fade, button-spinner, loading-skeletons, toast-config
- Tests are structured to fail meaningfully (RED phase) before features exist
- Test import paths match the file paths in plans 05-01 and 05-02
</success_criteria>

<output>
After completion, create `.planning/phases/05-polish/05-00-SUMMARY.md`
</output>
