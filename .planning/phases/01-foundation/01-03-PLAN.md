---
phase: 01-foundation
plan: 03
type: execute
wave: 3
depends_on: ["01-02"]
files_modified:
  - src/lib/db/schema.ts
  - src/actions/upload.ts
  - src/lib/supabase/storage.ts
  - src/components/upload/image-uploader.tsx
  - src/components/upload/upload-progress.tsx
  - src/app/(private)/upload/page.tsx
  - src/__tests__/upload/processing.test.ts
  - src/__tests__/upload/signed-urls.test.ts
autonomous: false
requirements:
  - IMG-01
  - IMG-02
  - IMG-03
  - IMG-04
must_haves:
  truths:
    - "A user can upload an image via drag-and-drop or file picker and it is automatically optimized with WebP variants at 400w, 800w, 1200w, and 1920w"
    - "Private images stored in the private-images bucket are only accessible via time-limited signed URLs"
    - "Upload supports batch selection of multiple files with per-file progress bars"
    - "Images load without layout shift because their dimensions are known before they render"
  artifacts:
    - path: "src/actions/upload.ts"
      provides: "Server Action for image upload with sharp processing"
      exports: ["uploadImage"]
    - path: "src/lib/supabase/storage.ts"
      provides: "Signed URL helpers for private images"
      exports: ["getSignedImageUrl", "getSignedImageUrls"]
    - path: "src/components/upload/image-uploader.tsx"
      provides: "Drag-and-drop upload zone with batch support"
      exports: ["ImageUploader"]
    - path: "src/components/upload/upload-progress.tsx"
      provides: "Per-file progress bar component"
      exports: ["UploadProgress"]
    - path: "src/lib/db/schema.ts"
      provides: "images and imageVariants tables added to existing schema"
      contains: "imageVariants"
  key_links:
    - from: "src/components/upload/image-uploader.tsx"
      to: "src/actions/upload.ts"
      via: "Server Action call with FormData"
      pattern: "import.*uploadImage.*actions/upload"
    - from: "src/actions/upload.ts"
      to: "src/lib/db/schema.ts"
      via: "Drizzle insert for images and imageVariants"
      pattern: "db\\.insert\\(images\\)"
    - from: "src/actions/upload.ts"
      to: "sharp"
      via: "Image processing"
      pattern: "sharp\\(buffer\\)"
    - from: "src/lib/supabase/storage.ts"
      to: "Supabase Storage"
      via: "createSignedUrl for private bucket"
      pattern: "createSignedUrl"
---

<objective>
Implement the complete image upload pipeline: sharp-based server-side processing that generates WebP variants at multiple sizes, Supabase Storage upload to public and private buckets, signed URL generation for private images, react-dropzone batch upload component with per-file progress, and a test upload page.

Purpose: Every feature in the app (portfolio, beauty tracker, journal) uploads and displays images. This pipeline is reused everywhere. The signed URL pattern ensures private images (journal, beauty) are only accessible to authenticated users.

Output: Working image upload with drag-and-drop, automatic optimization, variant generation, and private image access control.
</objective>

<execution_context>
@C:/Users/ADMIN/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/ADMIN/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-RESEARCH.md
@.planning/phases/01-foundation/01-UI-SPEC.md
@.planning/phases/01-foundation/01-02-SUMMARY.md

<interfaces>
<!-- From Plan 01/02: types and utilities needed -->
From src/lib/db/schema.ts (Plan 02 created):
export const inviteCodes = pgTable("invite_codes", { ... });
export const profiles = pgTable("profiles", { ... });

From src/lib/db/index.ts:
export const db: DrizzleInstance;

From src/lib/supabase/server.ts:
export async function createClient(): Promise<SupabaseClient>;

From src/lib/utils.ts:
export function cn(...inputs: ClassValue[]): string;
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add image tables to Drizzle schema, create upload Server Action with sharp processing, and create signed URL helpers</name>
  <files>
    src/lib/db/schema.ts,
    src/actions/upload.ts,
    src/lib/supabase/storage.ts,
    src/__tests__/upload/processing.test.ts,
    src/__tests__/upload/signed-urls.test.ts
  </files>
  <read_first>
    .planning/phases/01-foundation/01-RESEARCH.md,
    src/lib/db/schema.ts,
    src/lib/db/index.ts,
    src/lib/supabase/server.ts,
    src/lib/supabase/admin.ts
  </read_first>
  <action>
1. Update `src/lib/db/schema.ts` -- ADD these tables (keep existing inviteCodes and profiles):
   ```typescript
   export const images = pgTable("images", {
     id: uuid("id").defaultRandom().primaryKey(),
     originalName: text("original_name").notNull(),
     bucket: text("bucket").notNull(), // "public-images" or "private-images"
     folder: text("folder").notNull(), // e.g., "portfolio", "journal", "beauty"
     width: integer("width").notNull(),
     height: integer("height").notNull(),
     format: text("format").notNull(), // original format: jpeg, png, webp
     uploadedBy: text("uploaded_by").notNull(), // Supabase Auth user ID
     createdAt: timestamp("created_at").defaultNow().notNull(),
   });

   export const imageVariants = pgTable("image_variants", {
     id: uuid("id").defaultRandom().primaryKey(),
     imageId: uuid("image_id").notNull().references(() => images.id, { onDelete: "cascade" }),
     variantName: text("variant_name").notNull(), // "thumb", "medium", "large", "full"
     width: integer("width").notNull(),
     height: integer("height").notNull(),
     storagePath: text("storage_path").notNull(),
     sizeBytes: integer("size_bytes").notNull(),
     createdAt: timestamp("created_at").defaultNow().notNull(),
   });
   ```

2. Create `src/actions/upload.ts` with the `uploadImage` Server Action:
   - `"use server"` directive
   - Define SIZE_VARIANTS constant: `[{ name: "thumb", width: 400 }, { name: "medium", width: 800 }, { name: "large", width: 1200 }, { name: "full", width: 1920 }]`
   - Export `async function uploadImage(formData: FormData, options: { bucket: "public-images" | "private-images"; folder: string })`:
     a. Auth check: call `createClient()` then `supabase.auth.getUser()`, throw "Unauthorized" if no user
     b. Extract file from FormData: `formData.get("file") as File`
     c. Convert to buffer: `Buffer.from(await file.arrayBuffer())`
     d. Validate file type with sharp magic bytes: `const metadata = await sharp(buffer).metadata()`, reject if format not in ["jpeg", "png", "webp"]
     e. Generate UUID for image: `const imageId = randomUUID()`
     f. For each size variant: skip if variant width >= original width AND variant is not "full", resize with `sharp(buffer).resize(variant.width, undefined, { withoutEnlargement: true }).webp({ quality: 80 }).toBuffer()`, upload to Supabase Storage at path `{folder}/{imageId}/{variantName}.webp`, get resulting dimensions from `sharp(processed).metadata()`
     g. Insert into `images` table: id, originalName, bucket, folder, width, height, format, uploadedBy
     h. Insert each variant into `imageVariants` table: imageId, variantName, width, height, storagePath, sizeBytes
     i. Return `{ imageId, variants: [{ name, width, height, path, size }] }`
   - Import sharp, createClient from server, db from db/index, images/imageVariants from schema, randomUUID from crypto

3. Create `src/lib/supabase/storage.ts`:
   ```typescript
   import { createClient } from "@/lib/supabase/server";

   export async function getSignedImageUrl(path: string, expiresIn = 3600) {
     const supabase = await createClient();
     const { data, error } = await supabase.storage
       .from("private-images")
       .createSignedUrl(path, expiresIn);

     if (error) throw new Error(`Signed URL failed: ${error.message}`);
     return data.signedUrl;
   }

   export async function getSignedImageUrls(paths: string[], expiresIn = 3600) {
     const supabase = await createClient();
     const { data, error } = await supabase.storage
       .from("private-images")
       .createSignedUrls(paths, expiresIn);

     if (error) throw new Error(`Signed URLs failed: ${error.message}`);
     return data;
   }

   export function getPublicImageUrl(bucket: string, path: string) {
     return `${process.env.NEXT_PUBLIC_SUPABASE_URL}/storage/v1/object/public/${bucket}/${path}`;
   }
   ```

4. Create `src/__tests__/upload/processing.test.ts`:
   - Test that `src/actions/upload.ts` contains `sharp(buffer).metadata()` (magic byte validation)
   - Test that upload.ts contains all four variant names: "thumb", "medium", "large", "full"
   - Test that upload.ts contains variant widths: 400, 800, 1200, 1920
   - Test that upload.ts contains `.webp({ quality: 80 })` (WebP conversion)
   - Test that upload.ts contains `withoutEnlargement: true` (don't upscale)
   - Test that upload.ts contains `db.insert(images)` and `db.insert(imageVariants)`
   - Test that upload.ts contains `auth.getUser()` (auth check)
   - Use fs.readFileSync to read the source file

5. Create `src/__tests__/upload/signed-urls.test.ts`:
   - Test that `src/lib/supabase/storage.ts` contains `createSignedUrl`
   - Test that storage.ts contains `createSignedUrls` (batch)
   - Test that storage.ts contains `private-images` bucket name
   - Test that storage.ts exports `getSignedImageUrl` and `getSignedImageUrls` and `getPublicImageUrl`
   - Use fs.readFileSync
  </action>
  <verify>
    <automated>npx tsc --noEmit && npx vitest run src/__tests__/upload --reporter=verbose</automated>
  </verify>
  <acceptance_criteria>
    - src/lib/db/schema.ts contains `images` table with columns: id, originalName, bucket, folder, width, height, format, uploadedBy, createdAt
    - src/lib/db/schema.ts contains `imageVariants` table with columns: id, imageId, variantName, width, height, storagePath, sizeBytes
    - src/lib/db/schema.ts contains `onDelete: "cascade"` on imageVariants.imageId reference
    - src/actions/upload.ts contains `"use server"`
    - src/actions/upload.ts contains `sharp(buffer).metadata()` for magic byte validation
    - src/actions/upload.ts contains all four variant widths: `400`, `800`, `1200`, `1920`
    - src/actions/upload.ts contains `.webp({ quality: 80 })`
    - src/actions/upload.ts contains `withoutEnlargement: true`
    - src/actions/upload.ts contains `db.insert(images)` and `db.insert(imageVariants)`
    - src/actions/upload.ts contains `getUser()` auth check
    - src/lib/supabase/storage.ts contains `createSignedUrl` and `createSignedUrls`
    - src/lib/supabase/storage.ts contains `private-images`
    - src/lib/supabase/storage.ts contains `getPublicImageUrl`
    - `npx tsc --noEmit` exits 0
    - `npx vitest run src/__tests__/upload` exits 0
  </acceptance_criteria>
  <done>Image upload Server Action processes files through sharp to generate 4 size variants (400w, 800w, 1200w, 1920w) in WebP format. Validates file type by magic bytes. Stores variants in Supabase Storage (public or private bucket). Records image metadata including dimensions (width/height) for CLS prevention and variant details in database. Signed URL helpers serve private images with time-limited access. All upload and signed URL tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Create react-dropzone upload component with batch support, per-file progress bars, and upload test page</name>
  <files>
    src/components/upload/image-uploader.tsx,
    src/components/upload/upload-progress.tsx,
    src/app/(private)/upload/page.tsx
  </files>
  <read_first>
    .planning/phases/01-foundation/01-UI-SPEC.md,
    .planning/phases/01-foundation/01-CONTEXT.md,
    src/actions/upload.ts,
    src/components/ui/progress.tsx,
    src/components/ui/card.tsx,
    src/app/globals.css
  </read_first>
  <action>
1. Create `src/components/upload/upload-progress.tsx` ("use client"):
   - Export `UploadProgress` component
   - Props: `files: UploadFile[]` where UploadFile is `{ file: File; progress: number; status: "pending" | "uploading" | "complete" | "error"; error?: string }`
   - Export the `UploadFile` type
   - Render a vertical stack of file progress items
   - Each item: file name (14px Inter 700, truncated with ellipsis) + shadcn Progress bar (4px height, accent fill, rounded-full) + percentage text (14px Inter 400)
   - Complete status: green success checkmark (Lucide CheckCircle2 in text-success) replaces progress bar
   - Error status: red X (Lucide XCircle in text-destructive) + error message + "Retry" link in text-accent
   - Use exact colors from design tokens: success (#059669), destructive (#DC2626), accent (#E8B4B8)

2. Create `src/components/upload/image-uploader.tsx` ("use client"):
   - Export `ImageUploader` component
   - Props: `bucket: "public-images" | "private-images"` and `folder: string` and optional `onUploadComplete?: (imageId: string) => void`
   - Uses `useDropzone` from react-dropzone with accept: `{ "image/jpeg": [], "image/png": [], "image/webp": [] }`, multiple: true
   - Container styling per UI-SPEC:
     - Default: `border-2 border-dashed border-border rounded-[16px] p-12 text-center cursor-pointer`
     - Drag active: `border-accent border-solid bg-[rgba(232,180,184,0.05)]`
   - Center content: Lucide Upload icon (48px, text-text-secondary), "Drag photos here or click to browse" (16px Inter 400 text-text-secondary), "JPEG, PNG, WebP accepted" (14px Inter 400 text-text-secondary)
   - On drop: create UploadFile entries with status "pending", add to state
   - Upload logic: process files with max 3 concurrent uploads
   - For each file: set status to "uploading" with progress 50, create FormData, call `uploadImage(formData, { bucket, folder })`, on success set status "complete" progress 100, on error set status "error" with error message
   - After all files complete: show sonner toast "{count} photo(s) uploaded successfully" (via toast from sonner)
   - Below the drop zone: render UploadProgress component with current files
   - The drop zone itself is clickable (getRootProps makes it so), with hidden input via getInputProps

3. Create `src/app/(private)/upload/page.tsx`:
   - Server Component (no "use client")
   - Page heading: "Upload Photos" in Playfair Display (font-display text-2xl font-bold)
   - Subtitle: "Test the upload pipeline" in text-text-secondary
   - Render two ImageUploader instances for testing:
     a. "Public Upload" section with `bucket="public-images" folder="test"`
     b. "Private Upload" section with `bucket="private-images" folder="test"`
   - Each section has a heading and the ImageUploader component
   - This is a test page -- it will be removed or integrated into features in later phases
  </action>
  <verify>
    <automated>npx tsc --noEmit && npx next build</automated>
  </verify>
  <acceptance_criteria>
    - src/components/upload/upload-progress.tsx contains `UploadFile` type export with status union type `"pending" | "uploading" | "complete" | "error"`
    - src/components/upload/upload-progress.tsx contains `CheckCircle2` (success icon) and `XCircle` (error icon) from Lucide
    - src/components/upload/image-uploader.tsx contains `useDropzone` import from `react-dropzone`
    - src/components/upload/image-uploader.tsx contains `image/jpeg` and `image/png` and `image/webp` in accept config
    - src/components/upload/image-uploader.tsx contains `multiple: true`
    - src/components/upload/image-uploader.tsx contains `Drag photos here or click to browse`
    - src/components/upload/image-uploader.tsx contains `JPEG, PNG, WebP accepted`
    - src/components/upload/image-uploader.tsx contains `border-dashed` and `border-accent` (drag active state)
    - src/components/upload/image-uploader.tsx contains `uploadImage` import from actions
    - src/components/upload/image-uploader.tsx contains concurrent upload limit (e.g., `3` for max concurrent)
    - src/app/(private)/upload/page.tsx contains `ImageUploader` and `public-images` and `private-images`
    - `npx tsc --noEmit` exits 0
    - `npx next build` exits 0
  </acceptance_criteria>
  <done>Drag-and-drop upload zone renders with design system styling (dashed border, accent on drag, Upload icon). Clicking the zone opens file picker. Multiple files can be selected. Each file shows a progress bar during upload with status indicators. Files upload to the Server Action with max 3 concurrent. Success shows green check, errors show red X with retry. Toast notification shows total count on completion.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify complete Phase 1 foundation</name>
  <files>none</files>
  <action>
    Human verification checkpoint. All automated work is complete in Tasks 1-2. This task verifies the full Phase 1 foundation end-to-end.
  </action>
  <what-built>
    Complete Phase 1 foundation: design system with Tailwind v4 tokens, responsive navigation (top nav desktop / bottom tab bar mobile), invite-code account setup, email/password login, route protection, and image upload pipeline with sharp processing and per-file progress.
  </what-built>
  <how-to-verify>
    1. Start the dev server: `npm run dev`
    2. **Design System:** Open http://localhost:3000 -- verify lavender-tinted background (#F8F6FF), Playfair Display headings, Inter body text, rose gold accent elements, soft rounded corners
    3. **Responsive Nav:** On desktop (>768px), verify top nav bar with "Funnghy's World" logo left and links right. Resize below 768px -- top nav should hide, fixed bottom tab bar should appear with icon + label tabs
    4. **Public Routes:** Visit / and /about without logging in -- should render placeholder pages with public nav (Portfolio, About, Sign In)
    5. **Route Protection:** Visit /dashboard without login -- should redirect to /login
    6. **Login Page:** Visit /login -- verify elegant centered layout with "Funnghy's World" in rose gold serif at top, email/password form below, "First time?" invite code link at bottom
    7. **Invite Code Setup:** Visit /setup -- enter a valid invite code, verify it transitions to account creation form with email + password fields
    8. **Login Flow:** After creating account (or using existing), log in with email/password -- should redirect to dashboard showing "Welcome back, [Name]"
    9. **Auth Nav:** After login, verify nav shows all links (Portfolio, Beauty, Journal, About) + user menu with initial circle
    10. **Upload:** Navigate to /upload -- verify drag-and-drop zone with "Drag photos here or click to browse" text. Drop or select multiple images -- verify per-file progress bars appear, images process successfully
    11. **Logout:** Click user menu -> Sign Out -> confirm -> should redirect to /login

    NOTE: Requires Supabase project to be set up with correct env vars and public signup disabled. If Supabase is not configured, verify UI rendering and build success instead.
  </how-to-verify>
  <verify>Human visual and functional verification of the complete Phase 1 foundation</verify>
  <done>User has approved the Phase 1 foundation: design system, navigation, auth, and image upload all working correctly.</done>
  <resume-signal>Type "approved" to complete Phase 1, or describe any issues to fix</resume-signal>
</task>

</tasks>

<verification>
- `npx vitest run --reporter=verbose` -- all tests pass (design tokens, middleware, auth, upload, signed URLs)
- `npx next build` -- build succeeds
- Upload page: drag image onto zone -> progress bar -> complete with green check
- Upload page: click zone -> file picker -> select multiple -> batch upload
- Private upload: images stored in private bucket, accessible only via signed URLs
- Public upload: images stored in public bucket, accessible via public URLs
</verification>

<success_criteria>
- Image upload Server Action generates 4 WebP variants (400w, 800w, 1200w, 1920w)
- Sharp validates file type by magic bytes, not just extension
- Image dimensions stored in database at upload time for CLS-free rendering
- Private images use Supabase Storage signed URLs with expiration
- Upload component supports drag-and-drop AND click-to-browse
- Batch upload works with per-file progress bars (max 3 concurrent)
- Upload test page accessible at /upload (private route, requires auth)
- All tests pass, build succeeds
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-03-SUMMARY.md`
</output>
