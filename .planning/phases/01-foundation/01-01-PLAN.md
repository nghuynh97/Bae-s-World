---
phase: 01-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/globals.css
  - src/app/layout.tsx
  - src/app/(public)/layout.tsx
  - src/app/(public)/page.tsx
  - src/app/(public)/about/page.tsx
  - src/app/(private)/layout.tsx
  - src/app/(private)/dashboard/page.tsx
  - src/app/(auth)/login/page.tsx
  - src/app/(auth)/setup/page.tsx
  - src/components/layout/top-nav.tsx
  - src/components/layout/bottom-tab-bar.tsx
  - src/components/layout/logo-text.tsx
  - src/lib/utils.ts
  - next.config.ts
  - .env.example
  - .env.local
  - vitest.config.ts
  - src/__tests__/setup.ts
  - src/__tests__/design-tokens/tokens.test.ts
autonomous: true
requirements:
  - DESG-01
  - DESG-02
  - DESG-03
must_haves:
  truths:
    - "The app displays with the soft feminine design system (pastel tones, rose gold accents, cool white-lavender backgrounds, Playfair Display headings, Inter body text) on both mobile and desktop"
    - "Desktop viewport shows a horizontal top nav bar with logo left and nav links right"
    - "Mobile viewport shows a fixed bottom tab bar with icon + label tabs"
    - "Public routes (/, /about) render without requiring login"
    - "Private route placeholders (/dashboard) exist in the (private) route group"
    - "Auth route placeholders (/login, /setup) exist in the (auth) route group"
  artifacts:
    - path: "src/app/globals.css"
      provides: "Tailwind v4 @theme design tokens"
      contains: "@theme"
    - path: "src/app/layout.tsx"
      provides: "Root layout with Playfair Display + Inter fonts"
      contains: "Playfair_Display"
    - path: "src/components/layout/top-nav.tsx"
      provides: "Desktop navigation bar"
      exports: ["TopNav"]
    - path: "src/components/layout/bottom-tab-bar.tsx"
      provides: "Mobile bottom tab navigation"
      exports: ["BottomTabBar"]
    - path: "src/components/layout/logo-text.tsx"
      provides: "Branded logo text component"
      exports: ["LogoText"]
  key_links:
    - from: "src/app/layout.tsx"
      to: "src/app/globals.css"
      via: "CSS import"
      pattern: "import.*globals.css"
    - from: "src/app/(public)/layout.tsx"
      to: "src/components/layout/top-nav.tsx"
      via: "component import"
      pattern: "import.*TopNav"
    - from: "src/app/(private)/layout.tsx"
      to: "src/components/layout/top-nav.tsx"
      via: "component import"
      pattern: "import.*TopNav"
---

<objective>
Scaffold the Next.js project with all dependencies, establish the design system via Tailwind v4 @theme tokens, initialize shadcn/ui, create the navigation components (desktop top nav + mobile bottom tab bar), set up route groups for public/private/auth, and create placeholder pages for all Phase 1 routes.

Purpose: Everything in Phase 1 (and all future phases) depends on the project existing with the correct design system and layout structure. This plan creates the skeleton that auth and image upload build into.

Output: A running Next.js app with the full design system applied, responsive navigation, and placeholder pages for all routes.
</objective>

<execution_context>
@C:/Users/ADMIN/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/ADMIN/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-RESEARCH.md
@.planning/phases/01-foundation/01-UI-SPEC.md
@.planning/phases/01-foundation/01-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold Next.js project, install all dependencies, configure Tailwind v4 design tokens, initialize shadcn/ui, and set up root layout with fonts</name>
  <files>
    src/app/globals.css,
    src/app/layout.tsx,
    src/lib/utils.ts,
    next.config.ts,
    .env.example,
    .env.local,
    vitest.config.ts,
    src/__tests__/setup.ts,
    src/__tests__/design-tokens/tokens.test.ts
  </files>
  <read_first>
    .planning/phases/01-foundation/01-UI-SPEC.md,
    .planning/phases/01-foundation/01-RESEARCH.md,
    .planning/phases/01-foundation/01-CONTEXT.md
  </read_first>
  <action>
1. Create the Next.js project:
   ```bash
   npx create-next-app@latest . --typescript --tailwind --app --turbopack --eslint
   ```
   If the directory is not empty, initialize in a subdirectory and move files up, or use `--yes` flags as needed.

2. Install all Phase 1 dependencies:
   ```bash
   npm install @supabase/supabase-js @supabase/ssr drizzle-orm postgres sharp react-dropzone react-hook-form zod @hookform/resolvers
   npm install -D drizzle-kit @types/node vitest @vitejs/plugin-react jsdom @testing-library/react @testing-library/dom vite-tsconfig-paths
   ```

3. Initialize shadcn/ui:
   ```bash
   npx shadcn@latest init
   ```
   Then add components needed for Phase 1:
   ```bash
   npx shadcn@latest add button input label card sonner skeleton separator dropdown-menu progress
   ```

4. Create `.env.example` with these exact keys:
   ```
   NEXT_PUBLIC_SUPABASE_URL=your-project-url
   NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key
   SUPABASE_SERVICE_ROLE_KEY=your-service-role-key
   DATABASE_URL=postgresql://postgres:[password]@db.[project-ref].supabase.co:5432/postgres
   ```

5. Create `.env.local` with placeholder values matching `.env.example`.

6. Replace `src/app/globals.css` with the exact design tokens from the UI-SPEC. The file must contain:
   ```css
   @import "tailwindcss";

   @theme {
     --color-dominant: #F8F6FF;
     --color-surface: #FFFFFF;
     --color-accent: #E8B4B8;
     --color-accent-hover: #D4A0A5;
     --color-destructive: #DC2626;
     --color-success: #059669;

     --color-text-primary: #2D2235;
     --color-text-secondary: #6B5F76;
     --color-border: #E8E4EE;
     --color-border-accent: #E8B4B8;
     --color-bg-hover: #F0ECF5;
     --color-shadow: rgba(232, 180, 184, 0.12);

     --font-display: 'Playfair Display', serif;
     --font-body: 'Inter', sans-serif;

     --radius-sm: 6px;
     --radius-md: 10px;
     --radius-lg: 16px;

     --shadow-sm: 0 1px 3px rgba(232, 180, 184, 0.08);
     --shadow-md: 0 4px 12px rgba(232, 180, 184, 0.12);
     --shadow-lg: 0 8px 24px rgba(232, 180, 184, 0.16);
   }

   body {
     font-family: var(--font-body);
     color: var(--color-text-primary);
     background-color: var(--color-dominant);
   }
   ```

   If shadcn/ui init adds its own CSS variables or @theme block, merge them. The design token values above take precedence.

7. Create `src/app/layout.tsx` as root layout:
   - Import Playfair_Display and Inter from `next/font/google`
   - Configure Playfair_Display: subsets: ['latin'], weight: ['400', '700'], variable: '--font-display', display: 'swap'
   - Configure Inter: subsets: ['latin'], weight: ['400', '700'], variable: '--font-body', display: 'swap'
   - Apply both font variables to the html element className
   - Import globals.css
   - Wrap children in a Toaster (from sonner/shadcn) for toast notifications
   - Set html lang="en"
   - Set metadata: title "Funnghy's World", description "A personal space for beauty, memories, and creativity"

8. Update `next.config.ts` to include Supabase Storage remote patterns:
   ```typescript
   const nextConfig = {
     images: {
       remotePatterns: [
         {
           protocol: "https" as const,
           hostname: "*.supabase.co",
           pathname: "/storage/v1/object/**",
         },
       ],
     },
     serverActions: {
       bodySizeLimit: "50mb",
     },
   };
   ```

9. Ensure `src/lib/utils.ts` has the `cn` utility (shadcn should create this):
   ```typescript
   import { clsx, type ClassValue } from "clsx";
   import { twMerge } from "tailwind-merge";
   export function cn(...inputs: ClassValue[]) { return twMerge(clsx(inputs)); }
   ```

10. Create `vitest.config.ts`:
    ```typescript
    import { defineConfig } from "vitest/config";
    import react from "@vitejs/plugin-react";
    import tsconfigPaths from "vite-tsconfig-paths";

    export default defineConfig({
      plugins: [react(), tsconfigPaths()],
      test: {
        environment: "jsdom",
        setupFiles: ["./src/__tests__/setup.ts"],
        include: ["src/__tests__/**/*.test.ts", "src/__tests__/**/*.test.tsx"],
      },
    });
    ```

11. Create `src/__tests__/setup.ts`:
    ```typescript
    import "@testing-library/dom";
    ```

12. Create `src/__tests__/design-tokens/tokens.test.ts` that reads globals.css and verifies all design token values are present:
    - Test that globals.css contains `--color-dominant: #F8F6FF`
    - Test that globals.css contains `--color-accent: #E8B4B8`
    - Test that globals.css contains `--font-display: 'Playfair Display'`
    - Test that globals.css contains `--font-body: 'Inter'`
    - Test that globals.css contains `--radius-md: 10px`
    - Test that globals.css contains `--shadow-md:`
    Use `fs.readFileSync` to read the CSS file and assert with string includes.
  </action>
  <verify>
    <automated>npx vitest run src/__tests__/design-tokens/tokens.test.ts --reporter=verbose</automated>
  </verify>
  <acceptance_criteria>
    - globals.css contains `--color-dominant: #F8F6FF`
    - globals.css contains `--color-accent: #E8B4B8`
    - globals.css contains `--color-accent-hover: #D4A0A5`
    - globals.css contains `--color-text-primary: #2D2235`
    - globals.css contains `--font-display: 'Playfair Display', serif`
    - globals.css contains `--font-body: 'Inter', sans-serif`
    - globals.css contains `--radius-sm: 6px`
    - globals.css contains `--radius-md: 10px`
    - globals.css contains `--radius-lg: 16px`
    - globals.css contains `--shadow-sm:`
    - globals.css contains `--shadow-md:`
    - globals.css contains `--shadow-lg:`
    - layout.tsx contains `Playfair_Display`
    - layout.tsx contains `Inter`
    - layout.tsx contains `--font-display`
    - layout.tsx contains `--font-body`
    - next.config.ts contains `*.supabase.co`
    - next.config.ts contains `bodySizeLimit`
    - .env.example contains `NEXT_PUBLIC_SUPABASE_URL`
    - .env.example contains `SUPABASE_SERVICE_ROLE_KEY`
    - .env.example contains `DATABASE_URL`
    - vitest.config.ts exists
    - `npx vitest run src/__tests__/design-tokens/tokens.test.ts` exits 0
    - `npm run build` succeeds (or `npx next build` if no script configured)
  </acceptance_criteria>
  <done>Next.js project scaffolded with all dependencies, Tailwind v4 design tokens matching UI-SPEC exactly, shadcn/ui initialized with Phase 1 components, fonts configured, Supabase remotePatterns in next.config, env files created, Vitest configured with design token tests passing.</done>
</task>

<task type="auto">
  <name>Task 2: Create navigation components (TopNav, BottomTabBar, LogoText), route group layouts, and placeholder pages</name>
  <files>
    src/components/layout/logo-text.tsx,
    src/components/layout/top-nav.tsx,
    src/components/layout/bottom-tab-bar.tsx,
    src/app/(public)/layout.tsx,
    src/app/(public)/page.tsx,
    src/app/(public)/about/page.tsx,
    src/app/(private)/layout.tsx,
    src/app/(private)/dashboard/page.tsx,
    src/app/(auth)/login/page.tsx,
    src/app/(auth)/setup/page.tsx
  </files>
  <read_first>
    .planning/phases/01-foundation/01-UI-SPEC.md,
    .planning/phases/01-foundation/01-CONTEXT.md,
    src/app/globals.css,
    src/app/layout.tsx,
    src/lib/utils.ts
  </read_first>
  <action>
1. Create `src/components/layout/logo-text.tsx`:
   - Export `LogoText` component
   - Renders "Funnghy's World" text
   - Uses `font-display` (Playfair Display) via Tailwind class
   - Text color: `text-accent` (#E8B4B8)
   - Accept optional `size` prop: "sm" (24px) | "lg" (36px, Display size), default "lg"
   - The "sm" variant is for nav bar, "lg" for login page hero

2. Create `src/components/layout/top-nav.tsx`:
   - Export `TopNav` component
   - Accept `isAuthenticated` boolean prop (default false) and optional `userName` string prop
   - Height: 64px (h-16)
   - Background: white (#FFFFFF surface) with shadow-sm bottom
   - Max content width: 1280px (max-w-7xl), centered with auto margins
   - Horizontal padding: 32px (px-8)
   - Left: LogoText size="sm"
   - Right: nav links using Next.js `Link` component
   - Public visitors see: "Portfolio" (href="/"), "About" (href="/about"), "Sign In" button (href="/login")
   - Authenticated users see: "Portfolio" (href="/"), "Beauty" (href="/beauty"), "Journal" (href="/journal"), "About" (href="/about"), and a user dropdown (placeholder div with user initial circle for now)
   - Nav link style: `text-sm font-normal text-text-secondary hover:text-text-primary` with accent underline on active (use `usePathname()` to detect)
   - Active link has 2px bottom border in accent color
   - Hidden on mobile: `hidden md:flex`
   - "use client" directive needed for usePathname

3. Create `src/components/layout/bottom-tab-bar.tsx`:
   - Export `BottomTabBar` component
   - Accept `isAuthenticated` boolean prop (default false)
   - Fixed to bottom: `fixed bottom-0 left-0 right-0`
   - Height: 56px (h-14)
   - Background: white with shadow-lg top (use `shadow-[0_-8px_24px_rgba(232,180,184,0.16)]`)
   - z-index: 50
   - Hidden on desktop: `md:hidden`
   - Public tabs (3): Portfolio (Image icon), About (Info icon), Sign In (LogIn icon) -- use Lucide icons
   - Authenticated tabs (4): Portfolio (Image icon, href="/"), Beauty (Sparkles icon, href="/beauty"), Journal (BookOpen icon, href="/journal"), Profile (User icon, href="/dashboard")
   - Each tab: flex column, Lucide icon 24px above label text (14px Inter 400)
   - Active tab: icon + label in accent color, 4px diameter dot in accent below icon
   - Inactive tab: icon + label in text-secondary
   - Tabs are Next.js Link components
   - "use client" directive needed for usePathname

4. Create `src/app/(public)/layout.tsx`:
   - Import TopNav and BottomTabBar
   - Render TopNav with isAuthenticated={false} (hardcoded for now, Plan 02 makes this dynamic)
   - Render BottomTabBar with isAuthenticated={false}
   - Main content area with padding-bottom 56px on mobile (for bottom tab bar clearance)
   - Max content width 1280px centered

5. Create `src/app/(public)/page.tsx`:
   - Portfolio placeholder page
   - Heading "Portfolio" in Playfair Display (font-display)
   - Subtitle "Coming soon" in text-secondary
   - Centered content

6. Create `src/app/(public)/about/page.tsx`:
   - About placeholder page
   - Heading "About" in Playfair Display
   - Subtitle "Coming soon" in text-secondary

7. Create `src/app/(private)/layout.tsx`:
   - Same structure as public layout but with isAuthenticated={true}
   - TopNav with isAuthenticated={true} and userName="User" (placeholder, Plan 02 makes dynamic)
   - BottomTabBar with isAuthenticated={true}
   - Main content area with same padding structure

8. Create `src/app/(private)/dashboard/page.tsx`:
   - Heading "Welcome back, User" in Playfair Display (24px heading size, font-display)
   - 3 dashboard cards in 2-column grid on desktop (grid-cols-2 gap-8), stacked on mobile (gap-6)
   - Each card uses shadcn Card component with: rounded-radius-lg (rounded-[16px]), shadow-md, white bg
   - Card content: section title (14px Inter), subtitle in text-secondary, accent-colored ChevronRight icon
   - Three cards: "Portfolio" / "Your public gallery", "Beauty Tracker" / "Products and routines", "Photo Journal" / "Daily memories"
   - Empty state: dashed accent border outline where thumbnail would be, "Nothing here yet" text
   - Cards link to their respective sections using Next.js Link

9. Create `src/app/(auth)/login/page.tsx`:
   - Full viewport height, centered content (min-h-screen flex items-center justify-center)
   - Background: dominant (#F8F6FF, inherited from body)
   - LogoText size="lg" at top
   - Subtitle: "Welcome to Funnghy's World" in text-secondary, body size
   - Below subtitle: "Sign in with your email and password to continue." in text-secondary
   - Placeholder for login form (just the container div, 360px max-width, centered)
   - Below form area: "First time? Enter your invite code" link (text-secondary, href="/setup")
   - This is a placeholder -- Plan 02 adds the actual form

10. Create `src/app/(auth)/setup/page.tsx`:
    - Same structure as login page
    - LogoText size="lg" at top
    - Subtitle: "Set Up Your Account"
    - Placeholder for invite code form (360px max-width container)
    - "Already have an account? Sign in" link (href="/login")
  </action>
  <verify>
    <automated>npx tsc --noEmit && npx next build</automated>
  </verify>
  <acceptance_criteria>
    - src/components/layout/logo-text.tsx contains `export` and `Funnghy's World` and `text-accent`
    - src/components/layout/top-nav.tsx contains `export` and `isAuthenticated` and `hidden md:flex`
    - src/components/layout/bottom-tab-bar.tsx contains `export` and `fixed bottom-0` and `md:hidden`
    - src/app/(public)/layout.tsx contains `TopNav` and `BottomTabBar`
    - src/app/(public)/page.tsx contains `Portfolio`
    - src/app/(public)/about/page.tsx contains `About`
    - src/app/(private)/layout.tsx contains `isAuthenticated={true}`
    - src/app/(private)/dashboard/page.tsx contains `Welcome back` and `Portfolio` and `Beauty Tracker` and `Photo Journal`
    - src/app/(auth)/login/page.tsx contains `LogoText` and `Welcome to Funnghy's World` and `invite code`
    - src/app/(auth)/setup/page.tsx contains `LogoText` and `Set Up Your Account`
    - `npx tsc --noEmit` exits 0
    - `npx next build` exits 0
  </acceptance_criteria>
  <done>All three route groups exist with placeholder pages. Desktop shows top nav bar with logo left and links right. Mobile shows fixed bottom tab bar with icon + label tabs. Login and setup pages have the elegant centered layout with LogoText hero. Dashboard shows 3 quick-access cards in responsive grid. Public nav shows Portfolio/About/Login, authenticated nav shows full links including Beauty and Journal.</done>
</task>

</tasks>

<verification>
- `npx vitest run --reporter=verbose` -- all tests pass (design tokens)
- `npx next build` -- build succeeds with no errors
- Navigate to `/` -- renders Portfolio placeholder with public nav
- Navigate to `/about` -- renders About placeholder
- Navigate to `/dashboard` -- renders dashboard with 3 cards and authenticated nav
- Navigate to `/login` -- renders centered login page with LogoText
- Navigate to `/setup` -- renders centered setup page with LogoText
- Resize viewport below 768px -- top nav hides, bottom tab bar appears
- Resize viewport above 768px -- bottom tab bar hides, top nav appears
</verification>

<success_criteria>
- Next.js project builds and runs with zero errors
- All Tailwind v4 design tokens match UI-SPEC hex values exactly
- Playfair Display and Inter fonts load correctly
- Three route groups exist: (public), (private), (auth)
- TopNav renders on desktop, BottomTabBar renders on mobile
- All placeholder pages render with correct headings
- Dashboard shows 3 quick-access cards in responsive grid
- Vitest design token tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-01-SUMMARY.md`
</output>
